# Managed by Ansible: created once, then left alone (force: false). Fill real values below.
# Do NOT commit real values anywhere in the repo.
DATABASE_URI={{ database_uri | default('CHANGEME-neon-pooler-connection-string?sslmode=require') }}
PAYLOAD_SECRET={{ payload_secret | default('CHANGEME-prod-secret-different-from-dev') }}
SERVER_URL={{ server_url }}
NEXT_PUBLIC_PLAUSIBLE_SRC=
# CR-04/CR-18: uploads live OUTSIDE releases/ — a release dir is wiped by the next deploy
# (rsync --delete + cleanup of old releases), taking every uploaded cover with it.
# Must be an ABSOLUTE path; the deploy workflow refuses to activate a release without it.
MEDIA_DIR={{ media_dir }}
