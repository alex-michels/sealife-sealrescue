# Install once on the control node:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general   # ufw module
  - name: ansible.posix       # authorized_key module
