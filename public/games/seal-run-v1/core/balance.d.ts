export const BAL: { SPEED_MAX: number; FISH_SPEED_BUFF_MULT: number; BURST_MULT: number; MAX_COURSE_MS: number; STARTING_LIVES: number };
export function computeScore(distanceM: number, fishPoints: number, livesRemaining: number): number;
