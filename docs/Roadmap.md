# Roadmap — sealife-sealrescue

Детальный план работ. Привязан к `COMPLIANCE_EU_DE.md`, `DESIGN_BRIEF.md` и инвариантам `CLAUDE.md`.
Задачи имеют ID (`M2-T05`) — ссылайтесь на них в чате с Claude Code.

**Легенда:** `[ ]` to do · `[x]` done · `[~]` in progress · `[!]` blocked
**Размер:** `S` ≤ полдня · `M` ≈ 1–2 дня · `L` ≈ неделя+
**Фокус (мастер-план):** сайт с контентом → агенты → игры/квизы → монетизация.

> ⚠️ Жёсткие гарантии (агент не публикует/не удаляет, минимизация данных) — в КОДЕ.
> **Бренд:** sealife — игривый/умиляющийся с тюль-сленгом; sealrescue — серьёзный. «Тюлента» = сообщества VK/TG, НЕ имя сайта.
> **Вордмарки:** sealife = «Тюлень.Инфо» (RU) / «SeaLife.Info» (EN); sealrescue = «Спасение тюленей» (RU) / «Seal Rescue» (EN).
> **Локали:** публичный контент — RU/EN (две равноправные контент-локали). `de` — **legal-only
> route-локаль**: под `/de` открываются только Impressum · Datenschutz · Cookies · Terms (немецкие
> тексты документов сохранены; оболочка страницы рендерится по-английски, немецкого UI-словаря нет),
> любой контент под `/de` → 404. German/EU legal pages нужны из-за юрисдикции оператора (Германия),
> а не из-за набора языков сайта. **TODO:** оставлять ли немецкие legal-страницы вообще — решение
> юрпроверки **EU-06**.

---

## Состояние на сейчас

> 📋 **Сверка роадмапа с кодом (аудит 2026-07-26).** Прогнан по всем ~200 ID: каждая задача
> проверялась чтением кода/тестов/конфигов, а не по галочке. Итог: **реально сделано 62** (галочек
> стояло 61 — совпадение случайное, состав разный). Найдено и здесь исправлено:
> **4 over-claim** (стояло `[x]`, сделано не полностью: M0-T13, M0-T19, SH-07, SH-08),
> **4 готовых без галочки** (M1-T19, M1-T25, M2-T05, QA-35),
> **~30 наполовину сделанных** с меткой «нетронуто» → переведены в `[~]` с точным остатком,
> **3 устаревших по решению** (M1-T06 → `[!]`, SH-10 переформулирован, доказательная база
> M0-T02/T03 держалась на закрытой альфе), **дубликат ID** M3-T05 (дашбордные «Переводы» →
> **M3-T12**). Приписки помечены «— **аудит 2026-07-26:**» прямо в задачах.
>
> Самая дорогая категория — не over-claim, а **наполовину сделанное под `[ ]`**: по такому списку
> легко начать переписывать работающий код. Крупнейшие примеры: M1-T15 (sitemap сдан, остался
> только `robots.txt`), M1-T16 (meta из `seo` работает, нет JSON-LD), M1-T20 (a11y реализована,
> не хватает проверки), EU-08 (consent сделан и покрыт e2e, нет лога согласий), M2-T09 (половина
> «нет publish/delete» закреплена тестами).
>
> Впервые проведён **предметный** аудит (морская биология, а не код) — результат вынесен в новый
> трек **BIO** ниже: пробелы в публичных инструкциях «нашёл тюленя» и ошибки в опубликованной
> биологии, включая приписывание однополушарного сна настоящим тюленям.

> 🧹 **Аудит мок-слоя и готовности к контенту (2026-07-26, после решения владельца наполнять сайты
> реальным контентом).** **M0-T19 закрыт** — последний невыполненный пункт («smoke-test навигации
> ссылок») доставлен как `tests/e2e/nav-smoke.e2e.spec.ts`. Два вывода вынесены в новые треки:
>
> **MOCK** — «моки больше не нужны» ≠ «папку можно удалить». `_components/mock/` вопреки имени
> содержит production-код (route-guard разделов с 13 импортёрами, fallback реального медиа, боевой
> error boundary), поэтому вывод идёт по одному разделу за раз, каждый — после payload-backed замены.
> Заодно всплыло, что три из четырёх пунктов навигации sealrescue — мок или заглушка, а `/rescue-news`
> вообще не имеет ни ID в этом файле, ни модели данных.
>
> **CR** — что ломается в момент появления реального текста, а не сеяных черновиков. Самое дорогое:
> глобальный `fallback: true` отдаёт русский текст под `hreflang="en"` (нарушение инварианта №3,
> срабатывает гарантированно при «пишу ru → перевожу позже»), а загруженные медиа не переживают
> деплой (нет storage-адаптера, `staticDir` резолвится в каталог релиза, который CI удаляет).


* [x] Контент-схема Payload (коллекции, RBAC, drafts, очередь `agent-proposals`, хуки).
* [x] `CLAUDE.md`/`AGENTS.md` в корне; доки проекта (`Roadmap.md`, `DESIGN_BRIEF.md`, `COMPLIANCE_EU_DE.md`, `DEPLOYMENT.md`, `INFRA.md` + тех-доки) в `docs/`.
* [x] ~~**Публичный alpha игры — live**~~ → **ВЫВЕДЕН ИЗ ЭКСПЛУАТАЦИИ
  2026-07-19/20 (PR #74).** Сервисы `sealife`+`caddy` на VPS остановлены и выключены, сайт-блок убран
  из `deploy/Caddyfile`, авто-деплой по push в `main` отключён. Инфра-as-code остаётся и переиспользуется
  под будущий QA-stage: `deploy/`, `infra/ansible/`, `.github/workflows/` (deploy/configure/seed/
  toggle-standalone/shutdown-alpha). Раннбук выключения и воскрешения — `DEPLOYMENT.md` §8;
  **фактическая инвентаризация бокса — `DEPLOYMENT.md` §1a** (бокс общий с сервисами владельца).
* [x] **Доменная политика (решение владельца 2026-07-26):** у проекта ровно **два** публичных корневых
  домена — `sealife.info` и `sealrescue.info` (+ любые их поддомены, напр. vanity `sealrun.sealife.info`).
  Игры живут роутами/поддоменами под ними; отдельные игровые домены больше не заводятся, домен
  выведенной альфы не используется и не возвращается.
* [x] **Немецкий убран как язык сайта (решение владельца 2026-07-26).** Контент-локалей теперь ровно
  две — `ru` и `en` (`src/i18n/config.ts`: `locales`), автоопределение языка выбирает только их
  (немецкий браузер → EN). `de` живёт как **legal-only route-локаль** (`legalOnlyLocales`/
  `routeLocales`/`isRouteLocale`): `/de/legal-notice` · `/de/privacy` · `/de/cookies` · `/de/terms`
  отвечают 200 с немецкими текстами `src/site/legal.ts`, все контентные роуты под `/de` → 404.
  Немецкий UI-словарь, DE-вордмарки («Robben.Info»/«Robbenrettung»), DE-моки и DE в сидах удалены;
  hreflang для контента — ru/en (`buildAlternates`), для legal — ru/en/de (`buildLegalAlternates`).
  Причина сохранения `/de`-документов — юрисдикция оператора (Германия): §5 DDG и §18 MStV не зависят
  от языка сайта. **TODO (EU-06):** оставлять ли немецкие legal-страницы вообще — на юрпроверку.

**Следующее:** контент силами агентов (M1-T06 переформулирован — импорта из VK/TG не будет);
перед публичным запуском — QA-stage (Environment-секреты, миграции вместо push, бэкапы),
заполненный legal-shell и юрпроверка. См. `DEPLOYMENT.md` §6–9.

> **Аудит документации 2026-07-01:** полный проход по всей кодовой базе против всех доков в `docs/` +
> корневых `.md`. Найденные расхождения исправлены прямо в тех-доках (`data-model.md`, `agents.md`,
> `api.md`, `localization.md`, `architecture.md`, `local-development.md`, `game-seal-hunter*.md`,
> `DEPLOYMENT.md`, `INFRA.md`); найденные пробелы в коде/тестах/инфре сведены сюда новыми пунктами
> (**SEC-05/07**, **QA-04…07**, уточнения **M0-T04/T05/T07**, **M1-T08**, **M2-T02**, **SH-06/SH-10**).
> Полные протоколы аудита — в истории PR этой ветки.

---

## M0 — Foundation / Setup (1–2 недели)

**DoD:** оба домена по HTTPS, RU/EN роутинг + legal-only `/de` (немецкие Impressum/Datenschutz/Cookies/Terms), cookie-consent + аналитика без cookies, секреты не в гите, бэкап БД, дизайн-токены подключены.

### Инфраструктура

* [x] **M0-T01** Postgres в EU/EEA (Neon, Frankfurt). *[S]*
* [~] **M0-T02** Деплой Payload+Next (EU-регион), прод + staging. *[M]* — alpha-пайплайн готов:
  CI собирает Next standalone → VPS (Contabo, EU) → Caddy + systemd, авто-деплой из `main`
  (`.github/workflows/deploy.yml`, `deploy/`). Остаётся: staging + prod-домены sealife/sealrescue. См. DEPLOYMENT.md.
  — попутно решить судьбу корневого `Dockerfile`: неиспользуемый Vercel-example boilerplate, реальный
  деплой (standalone + systemd) его не задействует нигде; либо пометить как reference-only, либо убрать
  (см. пометку в `DEPLOYMENT.md` §1, добавленную аудитом 2026-07-01).
  — **аудит 2026-07-26:** переформулировать: пайплайн больше не «готов и авто-деплоит» — push-триггер
  закомментирован, ни одно окружение не запущено. Разделить остаток на (а) QA-stage и (б) прод-домены.
  Health-check починен: цель задаётся входом `health_url`. Подпункт про корневой `Dockerfile` де-факто
  закрыт документацией (помечен как неиспользуемый boilerplate) — либо закрыть его, либо отметить в
  самом файле.
* [ ] **M0-T03** Домены (DNS, SSL, DDoS, кэш). *[M]* — на альфе отработаны DNS + авто-HTTPS Caddy
  (домен альфы выведен из эксплуатации, см. «Состояние на сейчас»). Осталось: `sealife.info` и
  `sealrescue.info` (+ их поддомены, напр. `sealrun.sealife.info` — SR-12) + DDoS/кэш. Других
  корневых доменов проект не заводит.
  — **аудит 2026-07-26:** откат к нулю по существу: `[~]` держался на закрытом домене альфы. Для
  sealife.info / sealrescue.info не сделано НИЧЕГО — в `deploy/Caddyfile` нет активных сайт-блоков,
  Caddy на боксе остановлен, сертификаты не продлеваются. Скоуп: DNS + TLS для двух апексов;
  CDN/DDoS/кэш — позже.
* [ ] **M0-T04** Media delivery: **Contabo Object Storage** (переплан с 2026-06-30, см. `INFRA.md` §4; было Hetzner+Bunny) + CDN Pull Zone + `assets.sealife.info` / `assets.sealrescue.info`; Sharp variants on upload; no provider URLs in CMS; AVIF/WebP/JPEG fallback; widths 320/640/960/1280/1920; game assets versioned; RU reachability test. *[M]* → PERF/SEO
* [~] **M0-T05** Секреты в secret manager; `.env` в `.gitignore`. *[S]* → SEC — `.env`/`.env*.local` уже в `.gitignore` (готово); секрет-менеджер (Vault/SOPS/облачный) — не подключён, секреты сейчас в GitHub Repository secrets + `/etc/sealife/.env` на боксе (см. `INFRA.md` §6).
* [ ] **M0-T06** Ежедневный бэкап Postgres + проверка восстановления. *[S]* → SEC — на alpha БД =
  **Neon EU** (бэкапы покрывает PITR Neon; данные анонимны, без PII). Собственный `pg_dump`-бэкап —
  **гейт перед self-hosted prod / любым PII** (DEPLOYMENT.md §7).
* [~] **M0-T07** CI: lint + typecheck + `generate:types` на PR. *[M]* → QA — lint + typecheck +
  `test:int` гейтят PR с QA-08 (`.github/workflows/test.yml`, 2026-07-02). Осталось: drift-чек
  `generate:types` (сгенерированные типы не разошлись со схемой) и pre-commit хук
  (husky/lint-staged) — либо снять формулировку хука из `CLAUDE.md`/`local-development.md`.
  — **аудит 2026-07-26:** CI-гейт сделан и ШИРЕ заявленного: `test.yml` гоняет на каждом PR lint +
  typecheck + `test:coverage` (unit+int) **и полный e2e** против прод-сборки. Остались: (1) drift-чек
  `generate:types`, (2) pre-commit хук либо снятие этого обещания.

### Роутинг и i18n

* [x] **M0-T08** Мультидомен в одном Next: sealife и sealrescue из общей CMS. *[M]*
* [x] **M0-T09** Локализованный роутинг `/ru/…`, `/en/…` (дефолт `ru`), БЕЗ forced-редиректа; текстовый свитчер RU/EN; legal-страницы дополнительно имеют DE-версию (`/de/legal-notice` и т.д.) БЕЗ `/de`-контента — контентные роуты под `/de` отдают 404. *[M]* → DESIGN
  — **аудит 2026-07-26:** сверх формулировки: контракт «контент на `/de` → 404, legal на `/de` → 200»
  закреплён e2e, немецкий браузер уходит на `/en`, устаревшая cookie `NEXT_LOCALE=de` игнорируется.
* [x] **M0-T10** `hreflang` + `x-default` + canonical + sitemap по локалям (контент — ru/en через `buildAlternates`; legal-роуты — ru/en/de через `buildLegalAlternates`). *[S]* → SEO
  — **аудит 2026-07-26:** два факта скоупа, чтобы их не приняли за сделанное: в sitemap нет legal-роутов
  (только главная + content + species) и **`robots.txt` не существует нигде** — он остался единственным
  содержанием M1-T15.

### Дизайн-фундамент (см. DESIGN_BRIEF.md)

* [x] **M0-T16** Токены в Tailwind + CSS-переменные: primitive → **semantic-слой** (используем в компонентах только его) → два режима через data-атрибут. *[M]* → DESIGN
* [x] **M0-T17** Шрифты через `next/font` (self-host, все с кириллицей): sealife Display = Unbounded/Rubik; sealrescue = без декор-display (заголовки тяжёлым Golos Text/Onest); Body = Onest/Golos Text; Mono = JetBrains Mono. НЕ Baloo 2. *[S]* → DESIGN
* [x] **M0-T18** Базовые примитивы: типошкала, кнопки, карточка, «усатый» разделитель, статус-точка, штамп проверки. *[M]* → DESIGN
* [x] **M0-T19** Кликабельный design mock / sample shell для ВСЕХ публичных страниц и разделов без реального контента: sample texts/fake records/placeholder media; sealife + sealrescue RU/EN; legal-shell RU/EN + legal-only DE; empty/loading/error/populated states; footer legal links + cookie settings; language switcher RU/EN; route guards (`/de` content → 404, `/de` legal → 200); равномерная card grid на sealife; smoke-test навигации ссылок. [L] → DESIGN/QA
  — **аудит 2026-07-26:** всё доставлено, КРОМЕ одного пункта формулировки — «smoke-test навигации
  ссылок»: ни один e2e не обходит ссылки навигации (`sectionDefs` проверяется только структурно в unit).
  — **закрыто 2026-07-26:** пункт доставлен — `tests/e2e/nav-smoke.e2e.spec.ts` (28 кейсов): меню в DOM
  сверяется с `navSectionsForSite()` (ни лишних пунктов, ни потерянных — так ловится мёртвая кнопка
  вроде DE в игре), каждая ссылка меню отвечает 200 с непустым единственным `<h1>`, legal-ссылки футера
  отвечают 200. Оба сайта × ru/en. Утверждений о текстах карточек нет сознательно: спека обязана
  пережить и моки, и реальные записи, и пустую БД.
  — **вывод из эксплуатации (решение владельца 2026-07-26):** design mock свою задачу выполнил, дальше
  инвестиций не будет — сайты наполняются реальным контентом. Но «моки больше не нужны» ≠ «папку можно
  удалить»: `_components/mock/` вопреки имени содержит **production-код** — `mockSection` (13
  route-импортёров, включая все пять уже payload-backed разделов), `PlaceholderMedia` (fallback
  реального медиа в `content/Cover.tsx`), `ErrorBlock` (боевой error boundary `error.tsx`),
  `MockBanner` (нужен, пока каталог центров выдуман), `RescueQuest` (переписан BIO-аудитом),
  `ReportForm`, `CenterCard`. `rm -rf` по этой папке роняет сборку всего публичного фронтенда.
  Sample-**данные** выводятся по одному разделу за раз, каждый — только после того, как приедет
  payload-backed замена. Порядок и развилки — трек **MOCK**, готовность к наполнению — трек **CR**.

### EU compliance (базис) — см. сквозной трек EU

* [x] **M0-T11** Cookie-consent баннер (opt-in, reject как accept, настройки из footer). См. **EU-08**. *[M]* → EU
* [x] **M0-T12** Аналитика: **no-cookie режим**, без localStorage/fingerprinting, IP-анонимизация, без cross-site, задокументировано в Privacy; любой non-essential трекинг — только после opt-in. (Plausible/Umami; self-host сам по себе НЕ делает законным без consent.) *[S]* → EU
  — **аудит 2026-07-26:** уточнение: аналитика сейчас инертна во всех окружениях
  (`NEXT_PUBLIC_PLAUSIBLE_SRC` пуст) — код гейта проверен e2e, но продукт не подключён. Заявленная
  «IP-анонимизация» нигде в тексте Privacy не описана: это поведение Plausible, а не наше
  задокументированное обещание.
* [~] **M0-T13** **Legal-страницы (RU/EN + legal-only DE; контент сайта — RU/EN):** Impressum/Anbieterkennzeichnung · Privacy Policy · Cookie Policy + Cookie-Settings · Terms · AI-Transparency note · контакт для запросов субъекта данных. Немецкие тексты (`src/site/legal.ts`) остаются после снятия DE как языка сайта; оболочка `/de`-страниц английская. Судьба немецких legal-страниц — **EU-06**. См. **EU-07**. *[M]* → EU
  — **аудит 2026-07-26:** legal-shell (Impressum/Privacy/Cookies+Settings/Terms, RU/EN + legal-only DE,
  ссылки в футере, draft-баннер) сделан, но **AI-Transparency note из формулировки задачи отсутствует**
  (grep по src/ пусто). Плюс все данные оператора — плейсхолдеры `[…]`: `[x]` здесь НЕ означает
  готовность к запуску (см. EU-07).
* [ ] **M0-T14** AVV/DPA-реестр под-процессоров: хостинг, БД, email, аналитика, AI-провайдеры, платежи, CDN/хранилище. См. **EU-09**. *[S]* → EU
* [ ] **M0-T15** MFA на админку Payload. *[S]* → SEC
  — **аудит 2026-07-26:** у Payload 3.x нет встроенного MFA (в `node_modules/payload/dist` ноль
  упоминаний totp/twoFactor) — это не тумблер в конфиге, а плагин/кастомная auth-стратегия или IdP перед
  `/admin`. Оценка `[S]` оптимистична, переоценить при планировании.

---

## M1 — Сайт с контентом (sealife.info) (4–6 недель)

**DoD:** ~20 evergreen + новости + мемы (RU/EN), уникальный дизайн применён, квизы и мини-игры работают, реакции, share-картинки, sitemap, перелинковка на sealrescue.

### Контент-рендеринг

* [x] **M1-T01** Шаблоны `Content` по type (article/news/meme/page), RU/EN. *[M]*
* [x] **M1-T02** Лента/каталог + мемная галерея с фильтрами. *[M]*
* [x] **M1-T03** «Тюленепедия» (карточки видов) + «Факт дня». *[M]*
* [x] **M1-T04** Блок перелинковки sealife → sealrescue (два регистра: «Как спасают тюленей?» / «Нашёл тюленя? Это не шутки»). *[S]*
* [x] **M1-T27** Редактируемый контент разделов (global `SectionContent`): editor может переопределять `title`/`intro`/обложку карточек разделов из админки, НЕ трогая роутинг. **Структура (`slug`/`site`/`nav`/`hasDetail`) остаётся в коде (`src/site/sections.ts`) — единственный источник правды для роутинга и route-guard'ов.** Реализация: Payload **global** (не коллекция); `slug` — `select` с опциями из `sectionDefs` (не свободный ввод); рендер берёт структуру из кода и накладывает overrides по slug, fallback на код при пустом значении → БД может только дополнять, не создавать/ломать разделы (рассинхрон невозможен). **Все контент-локали (ru/en, native localized).** Access: read public; update — editor/admin (create/delete у globals нет; агенту никогда). *[M]* → DESIGN — сделано 2026-07-02: global `section-content` + резолвер `src/site/sectionContent.ts` (чтение с `fallbackLocale: false` — пустая локаль берёт код, не русский текст); подключено в `requireSection`/`sectionMetadata`/`requireDetail` (все section-страницы) и в хаб разделов на главной (обложки карточек). **Заодно: admin-обложки карточек игр** — `games.coverImage` (upload) + `showCardCover` (выкл → плейсхолдер, картинка сохраняется), список игр и заставка страницы игры используют загруженную обложку; CDN/geo-отдача — **M0-T04**. Тесты: unit (мерж/`cardCover`) + int (access глобала, независимость локалей). Проверено в браузере: override intro + обложки на всех контент-локалях, hide-тумблер, cleanup.

### Дизайн / Фронтенд (см. DESIGN_BRIEF.md)

* [~] **M1-T05** Главная sealife: bento-хаб с фикс-иерархией, кинетичный хэдлайн, дудл-маскот. Тон игривый/умиляющийся, тюль-сленг в микрокопии. *[L]* → DESIGN
  — **аудит 2026-07-26:** каркас уже есть (hero + вордмарк + анимированный маскот, хаб разделов через
  `EqualCardGrid`, лента свежего, emergency-кросслинк). Осталось ровно две вещи из формулировки:
  **bento-раскладка с фикс-иерархией** и **кинетичный хэдлайн**. Не планировать с нуля.
* [x] **M1-T19** Дудл-маскот (SVG) + ОДНА микрореакция (hover/load), без Lottie. *[M]* → DESIGN
  — **аудит 2026-07-26:** сделано: чистый инлайновый SVG-маскот без Lottie и без клиентского JS,
  микрореакции только в CSS, глушатся `prefers-reduced-motion`, анимируется лишь на hero sealife.
  Уточнение к формулировке: реализованы ДВЕ idle-реакции (моргание + блеп), а не одна по hover/load.
* [~] **M1-T20** Адаптив до мобайла, видимый focus, target size ≥24px, `prefers-reduced-motion`. *[M]* → DESIGN/EU
  — **аудит 2026-07-26:** реализовано в компонентном слое: `:focus-visible`, target ≥24px
  (`min-h-6`/`min-h-11` в Button/TopicFilter/SectionNav/LanguageSwitcher), адаптив `.card-grid` с
  брейкпоинтом 820px, `prefers-reduced-motion`. Не хватает **проверки**: ни одного a11y/responsive-теста
  для сайта (axe не подключён). Переформулировать в «проверить и закрыть дыры».

### Наполнение

* [!] **M1-T06** ~~Перенести лучший контент из VK/TG в CMS (RU)~~ → **ОТМЕНЕНО, заменено:
  контент пишется сразу на сайтах силами агентов.** Решение владельца 2026-07-26: в TG/VK-канале
  нет собственного контента — там репосты из чужих соцсетей (техничеcки это делает отдельный бот
  `sealgram`, см. ниже), поэтому переносить нечего и юридически нельзя (чужие авторские права).
  **Новая формулировка задачи:** конвейер авторского контента «агент пишет черновик → человек
  ревьюит → публикация», без импорта извне. Это НЕ снимает инвариант №1 (агент не публикует) и
  инвариант №5 (provenance-маркировка) — наоборот, делает их критическим путём.
  *[M]* → зависит от **M2-T06/M2-T09** (контракт вывода агента + запись в очередь) и **M1-T08/EU-11**
  (provenance-поля). Пока их нет — статьи пишутся вручную через админку.
  > **NB о `sealgram`:** отдельный репозиторий/сервис владельца (Docker на том же VPS, см.
  > `DEPLOYMENT.md` §1a), репостит чужой тюлений контент Instagram/VK → Telegram-канал.
  > **К этому проекту отношения не имеет и подключаться не будет** — ни как источник контента для
  > CMS, ни как часть AI-стека (его провайдер моделей к нашим агентам не относится). Канал ≠ сайт.
* [ ] **M1-T07** ~20 evergreen-статей (RU) — **собственные**, авторские (агент-черновик + human
  review, см. M1-T06). *[L]*
* [~] **M1-T08** Перевод **EN→RU → human review → publish** (направление развёрнуто 2026-07-26, **CR-14**) — единственное направление перевода: контент-локалей две, DE снят как язык сайта 2026-07-26 (legal-only `/de` переводом контента не покрывается). Хранить provenance: `aiTranslated`, `humanReviewed`, `reviewedBy`, `reviewedAt`, `sourceContentHash`; показывать user-facing метку. (+ поля в Content/Translation.) См. **EU-11**. *[M]* → EU
  — по пути учесть: хук `markTranslationsStale` (`src/hooks/contentHooks.ts`) сейчас подключён
  ТОЛЬКО к `Content`, но не к `Species`/`Quizzes`/`Games` (у них тоже локализованные поля + drafts);
  расширить на все коллекции с переводимым контентом или явно задокументировать, почему не нужно.
  — **схема 2026-07-26 (PR provenance):** СХЕМНАЯ часть сделана: общая фабрика `provenanceField()`
  (`src/fields/provenance.ts`) подключена к `content`/`species`/`quizzes`. Флаги авторства локализованы
  (ru-оригинал человеческий, en-перевод машинный — случай Art. 50(4)), подтверждение фактов — общее для
  документа; «человек проверил» закрыт `isEditorField` от агента. `sourceContentHash` намеренно НЕ
  заведён — дублировал бы `localeStatus[].sourceHash`. Осталось: сам процесс перевода RU→EN с ревью,
  расширение `markTranslationsStale` на species/quizzes и UI-отображение шкалы.
* [~] **M1-T09** Glossary/translation memory подключить к процессу перевода. *[S]*
  — **аудит 2026-07-26:** хранилище готово: коллекция `glossary` (ru-источник + локализованный перевод +
  категория + варианты), сид `seed:glossary`, инвариант в int-тесте. Отсутствует ТОЛЬКО потребитель —
  процесса перевода, к которому подключать, ещё нет (блокируется M1-T08 / эпиком агентов).

### 🎮 Игры и вовлечение (эпик)

* [ ] **M1-T10** Квиз: рендер `Quizzes`, прохождение, результат, рейтинг. *[M]*
  — **аудит мока 2026-07-26:** коллекция не отсутствует, а **осиротела**: `src/collections/Quizzes.ts`
  уже имеет локализованные title/description, `questions[]` → `options[]{text,isCorrect}` +
  explanation, drafts и provenance, а `Quiz` есть в `payload-types.ts` — но её никто не читает
  (рядом с `getContent`/`getSpecies`/`getGames` нет `getQuizzes`), никто не сеет, а мок-деталь
  рендерит один статичный вопрос неинтерактивными `<span>`. Следствие, которое стоит записать:
  редактор может сегодня создать и опубликовать квиз — на сайте не изменится ничего. Форма мока не
  портируется: у `SampleQuiz` `questions: number` и один `sample`, в коллекции такого нет.
  Задача — гейт для вывода `sampleQuizzes`/`findQuiz`.
* [ ] **M1-T21** «Миф или правда?» — карточки с объяснением. *[M]*
* [ ] **M1-T22** «Угадай вид» — по силуэту/усам, варианты ответа. *[M]*
* [ ] **M1-T23** **Генераторы** — «какой ты тюлень сегодня», «тюлений гороскоп», «переведи фразу в тюлений стиль». «Тюлений переводчик» — фраза → тюль-сленг (вирусный генератор, share). *[M]*
* [ ] **M1-T24** «Тюлень дня» — daily-механика со streak (факт+мем+вопрос). *[M]*
* [x] **M1-T25** Браузерная аркада «лови рыбу» (Seal Hunter). *[M]* — реализуется framework-free на **Canvas2D** (лёгкая, бюджет CWV, DESIGN-03); см. эпик **SH-** ниже. Phaser НЕ нужен именно для этой аркады, но остаётся в стеке для более «тяжёлых» игр — см. **SR-01..SR-15** (Seal Run, Phaser 4) и **M4-T05**.
  — **аудит 2026-07-26:** игра работает и доступна на sealife.info: рантайм в
  `public/games/seal-hunt-v1/`, документ в коллекции `games`, ленивый iframe на странице игры. Запись о
  реализации — эпик **SH-01..SH-14** (закрыт, кроме переформулированного SH-10).
* [ ] **M1-T26** Пасхалки → бейджи.
* [ ] **M1-T11** Share-картинки (Open Graph) для статей/квизов/игр (VK/TG/X). *[M]*

#### 🦭 Seal Hunter — редизайн мини-игры (Canvas2D, без Phaser)

> Игра «Тюль-Охотник / Seal The Hunter» (slug `seal-the-hunter`, рантайм в `public/games/seal-hunt-v1`).
> Решения: рендер — vanilla Canvas2D; честность — нормализация по короткой оси + балансовые
> инварианты, две грубые доски (mobile/desktop); счёт — server-authoritative и анонимный.
> Best practices: см. обсуждение (game-dev scaling + safe area; server-authoritative anti-cheat; DSA-премодерация).

* **Фаза 1 — Уборка и честность**
  * [x] **SH-01** Привести vendored-игру в порядок: убрать вложенную дублирующую `dist/`, починить/удалить `sw.js` (root-absolute пути не работают под subpath), сделать файлы в репо линтируемым источником правды. *[S]*
  * [x] **SH-02** Фиксированная симуляция: мир в логических единицах с **константной короткой осью** и клампом длинной (без «extend view»-преимущества); масштабируется только рендер. Баланс как device-independent инварианты: плотность добычи на площадь, время жизни рыбы на экране, время пересечения поля тюленем. Убирает зависимость скорости/очков от размера экрана. *[M]*
  * [x] **SH-03** HUD в safe-area (≈90% бокса), сохранить WCAG 2.2 AA: focus, target ≥24px, `prefers-reduced-motion`. *[S]* → DESIGN/EU
* **Фаза 2 — Арт**
  * [x] **SH-04** SVG-редизайн: векторные тюлень и рыбы/добыча через `Path2D`, тинт из **semantic-токенов** (Foggy Coastal Utility, оба режима). Заменить текущие примитивы. *[M]* → DESIGN
    — **аудит 2026-07-26:** мелочь по формулировке: сказано «через `Path2D`», но в игре объект `Path2D`
    не используется — рисование идёт прямыми командами контекста. Либо переформулировать, либо
    игнорировать.
* **Фаза 3 — Лидерборды**
  * [x] **SH-05** Коллекция Payload `game-scores`: анонимное имя + score + durationMs + deviceClass (грубо mobile/desktop, без пиксельных размеров — анти-fingerprint) + статус модерации. Явный access: публичная запись НЕ напрямую; `delete` — никогда `agent`. *[M]* → EU
  * [x] **SH-06** Server-authoritative submit-API (Next route): Zod-валидация, плаузибилити-капы (catches/сек, длительность ≈60с), rate-limit; запись через Payload local API; без PII в логах. *[M]* → SEC/EU
    — ⚠️ **уточнение (аудит 2026-07-01):** отдельного profanity-фильтра/лога модерации в коде нет —
    премодерация фактически не нужна, т.к. имя игрока НЕ свободный текст, а собирается из закрытого
    списка слов (`ADJ_EN`/`MOD_EN`/`NOUN_EN`/… в `src/endpoints/leaderboard.ts`), UGC там отсутствует.
    Формулировка задачи выше упрощена относительно исходной (убран несуществующий пункт).
  * [x] **SH-07** UI лидерборда: **одна** анонимная доска, показ **перцентиля/ранга**, недельный
    сезонный сброс, прозрачные правила подсчёта. *[M]* → DESIGN
    — **аудит 2026-07-26:** формулировка исправлена. Было «две грубые доски (mobile/desktop)» —
    этот сплит осознанно УДАЛЁН в SH-11 (в `game-scores` нет поля `board`, сервер лишь терпит
    legacy-`b` в старых токенах). Две задачи со статусом `[x]` описывали взаимоисключающие
    системы.
  * [~] **SH-08** Анти-чит-харднинг и подстройка баланса по анонимизированным распределениям очков (итеративно). *[S]* → SEC
    — **аудит 2026-07-26:** анти-чит-половина сделана по-настоящему (HMAC play-token с TTL и привязкой к
    игре, одноразовый nonce, `MIN_PLAY_MS`, статистические капы). Вторая половина — «подстройка баланса
    по анонимизированным распределениям очков» — не состоялась и пока невозможна: публичного окружения,
    которое давало бы трафик, больше нет.
* **Фаза 4 — Публичный alpha игры** *(выведен из эксплуатации 2026-07-19/20 — история)*
  * [x] **SH-09** Standalone-лендинг игры: определение «не во фрейме», переключатель языка RU/EN на
    стартовом экране (стартовый язык: `?lang=`→сохранённый→браузер; запись в `localStorage` только после
    явного выбора), пометка альфа-теста + контакт для фидбэка на старте и финале;
    встраиваемая версия не меняется. Деплой/Caddy-allowlist — DEPLOYMENT.md. *[M]* → EU/DESIGN
    — ⚠️ **NB (домен, 2026-07-26):** заметка про альфа-тест и контакт для фидбэка **удалены из игры**
    вместе с выведенным доменом (`#alphaNotice`/`#feedbackInvite` в `index.html`, ключи в `i18n.js`,
    ветка в `game.js`); в standalone остался только переключатель языка. Понадобится контакт снова —
    только точка контакта оператора из Impressum; новых адресов и доменов не заводим.
    — ⚠️ **NB (2026-07-26):** словарь игры (`public/games/seal-hunt-v1/i18n.js`, `core/alias.js`)
    самодостаточен и НЕ связан с локалями сайта, поэтому DE в самой игре остался после снятия
    немецкого как языка сайта. Убирать ли его — отдельное решение владельца (см. также QA-16).
    — **аудит 2026-07-26:** содержимое устарело и уже вычищено: альфа-заметка и контакт `feedback@`
    удалены вместе с закрытым доменом (2026-07-26), в игре остался только переключатель языка RU/EN.
  * [ ] **SH-10** ~~Legal-доступность standalone-домена альфы~~ — **исходная постановка закрыта
    выводом альфы (2026-07-19/20) и доменной политикой 2026-07-26** (только `sealife.info` /
    `sealrescue.info` + поддомены). Отдельного игрового origin'а больше нет и не будет: игра
    открывается роутом/iframe'ом сайта и наследует его футер с Impressum/Datenschutz, поэтому
    пункты «футер со ссылками внутри игры» и «расширить Caddy-allowlist на legal-роуты» **отпали**.
    Остаётся общесайтовый legal-долг (поэтому чекбокс не закрыт):
    1. **Impressum** — реальные имя + почтовый адрес + email (§5 DDG; служебный ящик недостаточен).
       Обновить `§5 TMG`→`§5 DDG`, если встречается.
    2. **Datenschutz** — секция про игру: `localStorage` (`seal_hunt_seed/best/sound/lang`), transient
       IP-rate-limit лидерборда (**не хранится**, legitimate interest), EU-хостинг (Contabo/Neon),
       серверные логи + ретеншн, аналитики нет.
    Cookie-баннер пока **НЕ нужен** (storage strictly-necessary/functional, аналитика выключена) —
    включить вместе с Plausible. Основание: §5 DDG + GDPR (IP = перс. данные); см. `COMPLIANCE_EU_DE.md`. *[M]* → EU
    — подтверждено аудитом (2026-07-01): оставшиеся пункты не реализованы (`src/site/legal.ts` —
    плейсхолдеры, Datenschutz не описывает `localStorage`/rate-limit игры). Статус `[ ]` верен.
  * [x] **SH-11** **Единая доска**: убрать деление лидерборда `desktop`/`mobile` везде (endpoint:
    Zod/токен/дедуп/ранг; коллекция `game-scores`: поле `board` удалено; клиент: без `detectBoard`
    и без табов; тесты/моки/доки). Причина — консистентность с Seal Run («равный горизонт»,
    `game-seal-run-spec.md` §1.2, делить доску нечем); остаточный портрет-vs-ландшафт спред
    Seal Hunter (~единицы %) принят осознанно. Обратная совместимость на окно деплоя: легаси
    `board` в теле/URL игнорируется, старые токены с `b` валидны; на неделе деплоя дубль-строки
    игрока сливаются лениво (max score). *[M]* → SEC/DESIGN
* **Фаза 5 — Дальнейшая честность и контент (backlog)**
  * [x] **SH-12** **Единый аспект поля** («равный горизонт» для Seal Hunter): играбельная зона
    фиксируется **16:9 (ландшафт) / 9:16 (портрет)** вместо клампа 2:1 — при единой доске (SH-11)
    убирает остаточный внутриориентационный спред catch-rate между экранами. **Механику НЕ трогать:**
    спавн, скорости, анимации, арт, коллизии — байт-в-байт как есть; меняется только
    вьюпорт-математика (`VIEW_CFG`/`computeWorld` в `core/balance.js`). Бордюр-«край бухты» станет
    видим заметно чаще (почти на всех экранах, кроме ровно 16:9 / 9:16) — **принято осознанно**,
    рендер бордюра уже существует. Остаточная разница форм 16:9-поле vs 9:16-поле (ландшафт vs
    портрет) остаётся — замерить харнессом и зафиксировать числа в `game-seal-hunter.md`. Гейт:
    `tools/fairness-sim.mjs` по всем профилям + осознанное обновление golden/fairness-порогов
    (QA-30/31); бамп `CACHE` в `sw.js`. *[M]* → DESIGN — сделано: `computeWorld` → фикс. мир
    960×540/540×960; замер N=80: все ландшафты байт-идентичны (92.2), все портреты (93.0),
    **разброс 0.8%** (было 5.5% при клампе); golden QA-30 не изменился (FHD = 960×540 в обеих
    моделях); fairness-пороги ужаты 15/10 → 5/3 + ассерт фикс. поля на всех профилях; бордюр
    generic по осям зазора (правок рендера не потребовалось); `CLAMP`-эксперимент харнесса →
    `ASPECT`; SW CACHE v15→v16. Попутно (тот же PR): RU-имя доски — **«Доска тюлидеров»**
    вместо «Лидерборд» (i18n: `lbTitle`, `lbOffline`).
  * [x] **SH-13** ⚡ **ОЧЕНЬ РЕДКАЯ добыча «lightning sea star»** (молниевая морская звезда): при
    поимке — бафф **×2 скорости тюленя на 3 с** (исходно 2 c; 2→3 после плейтеста владельца
    2026-07-03, fairness-замер N=80: разброс 0.2%). Спавн: **очень редкий**, случайное время и
    случайная позиция (отдельный редкий roll ВНЕ edge-shuffle-bag обычной добычи; максимум 1 за
    раунд). Вид: силуэт звезды с зигзаг-лучами, свечение (вспышки глушатся
    `prefers-reduced-motion`; различимость НЕ только цветом — силуэт). *[M]* → DESIGN — сделано,
    с апдейтами владельца (2026-07-03): **5 очков** вместо 1 (`eatCb(f)` + `sp.points`) и
    **магический звук «вуаля»** (`magic()`: арпеджио + колокольчик-шиммер) вместо обычного pop.
    Техника: `STAR` в balance.js (chance 0.10, окно 10–45 c, жизнь 9 c + warn-фейд, inset 70);
    расписание = ровно 4 RNG-броска на раунд независимо от исхода (стабильный поток → golden =
    f(seed), пересобран осознанно: 98→92 у сид-раунда); спавн в `spawnTick` (общий путь
    игры/харнесса); бафф в `stepSeal` (`seal.buffT`, скорость+разгон ×2) + аура-кольцо на
    тюлене (видимая длительность); звезда `still` — не убегает. Fairness N=80: разброс
    **0.4%** (звезда orientation-fair под CRN); серверные капы не задеты (+4 очка ≪ потолка).
    Контракты: `tests/unit/seal-hunt-star.unit.spec.ts` (8) +
    `tests/e2e/game-lightning-star.e2e.spec.ts` (ореол/яркость/фейд). SW CACHE v17→v18.
  * [x] **SH-14** **Kill-switch standalone-версий (обе игры)** — запрос владельца 2026-07-08
    («альфа игры окончена»): тумблер on/off в коде и админке. ON (по умолчанию) —
    игра как обычно; OFF — standalone-страница (прямой URL/домен) показывает заглушку «Coming
    soon»: Seal Hunter — attract-фон с плавающей рыбой (сим spawnTick/updatePrey, «ловца»
    подменяет точка за полем) БЕЗ тюленя/таймера/очков/имён; Seal Run — статичный подводный фон
    (render/art.js без Phaser) БЕЗ сущностей; надпись — cartoon-CSS (обводка+«наклейка»-тень,
    боб глушится reduced-motion). iframe на sealife.info флаг НЕ читает. Реализация: поле
    `games.standaloneComingSoon` (null у старых строк = игра доступна — пуш схемы ничего не
    гасит) + публичный `GET /api/game-config?game=` (кэш 60 с, draft-тумблер не влияет до
    Publish) + ветка в game.js обеих игр (fail-open при недоступном API; вуаль от вспышки меню).
    Тесты: `tests/int/game-config.int.spec.ts` (7) + `tests/e2e/game-placeholder.e2e.spec.ts`
    (route-мок конфига: заглушка/fail-open/iframe-иммунитет). Пока альфа была жива, `/api/game-config`
    держался в allowlist её прокси (иначе 404 → fail-open, тумблер не доехал бы).
    Переключить флаг без доступа к админке: Actions → **Toggle game standalone** →
    `coming_soon` (workflow пишет флаг в БД `DATABASE_URI`; локально —
    `npm run game:standalone -- coming_soon seal-the-hunter`); где админка доступна — тот же
    флаг в Games → Publish. Доезжает ≤ 60 с. *[M]*

#### 🕹 Phaser — «Seal Run» (референс: когда и как подключать Phaser)

> Phaser остаётся в стеке как осознанный выбор для игр со сценами/физикой/тайлмапами/множеством
> спрайтов. Правило выбора: лёгкая аркада (как Seal Hunter) → Canvas2D; богатая механика → Phaser.
> «Seal Run» (side-scroll раннер: тюлень уворачивается от хищников/мусора/камней, ловит рыбу,
> дистанция = очки) — эталонная **Phaser 4**-игра проекта. Обязательно: **lazy-load бандла**
> (динамический `import()` внутри статической игры, НЕ в общий Next-чанк — бюджет CWV/DESIGN-03),
> изоляция как у Canvas2D-игры (iframe/route на `sealife.info/[locale]/games/seal-run` + vanity-
> редирект `sealrun.sealife.info` → этот путь, БЕЗ отдельного standalone-домена в v1 — см.
> `docs/game-seal-run.md` §2.5), переиспользование анонимного server-authoritative лидерборда
> (SH-05/06/07, расширение полей — SR-09). Полный дизайн (биология/баланс/архитектура + аудит
> 2026-07-01) — [`docs/game-seal-run.md`](game-seal-run.md).
>
> **Порядок работ:** сначала **играбельный vertical slice** — SR-05 (минимальная сцена) + SR-03
> (sim-core) + один тип препятствия + одна рыба, **без арта / без лидерборда / без анти-чита** —
> чтобы проверить шов «Phaser 4 ↔ изоморфный sim-core» ДО вложений в SR-06 (арт) и SR-10 (анти-чит).

* **Фаза 1 — Дизайн и harness баланса**
  * [x] **SR-01** Спека механик: **управление — непрерывное свободное движение по Y** (тюлень плывёт
    к целевому Y со сглаживанием; касание/перетаскивание на мобильном, указатель/↑↓ на десктопе),
    контент авторится по дискретным Y-«полосам» (bands); 3 яруса препятствий (хищники −жизнь /
    сети-мусор−замедление / камни−отскок), **ростер v1 биом-точный** (`coastal`: орка + белая акула +
    крупная акула-вариант; белый медведь/морской леопард — v2 с их биомами); единый стамина/кислород-
    метр (восстановление рыбой); сид-детерминированная трасса (`courseSeed = hash(season)`, общая на
    неделю); фиксированная по дистанции сессия (не бесконечная). *[M]* → DESIGN —
    **нормативная спека: [`game-seal-run-spec.md`](game-seal-run-spec.md)** (единицы/детерминизм,
    формулы физики Y, автомат стамины, кинематика хищников, формат чанка + `generateCourse`,
    формула очков ≤100k, серверные проверки; константы предварительные — гейт SR-04)
  * [x] **SR-02** **Изоморфный** `core/course.js` + `core/chunks/*.js` (dependency-free ESM, без DOM/
    Phaser, целочисленный `mulberry32` — одни файлы для браузера И для Node-адаптера анти-чита SR-10):
    формат чанка (difficultyScore/biome/**bands**/obstacles/fish) + библиотека 15–25 авторских чанков
    (один biome `coastal` в v1) + `generateCourse(seed, biome)` сид-детерминированный сборщик. *[M]* —
    сделано: `public/games/seal-run-v1/core/{course.js,chunks/}` (18 чанков) + **chunk-lint**
    (`tools/chunk-lint{,-lib}.mjs`: проходимость safe-interval-BFS, коридоры, питание, бюджет рыбы)
    + CI-тест `tests/unit/seal-run-course.unit.spec.ts` (детерминизм + сборки по 20 сидам)
  * [x] **SR-03** `core/sim.js`+`core/balance.js` (DOM-free): физика тюленя по Y (**eased target-Y
    follow**, описать на собственных условиях), стамина/жизни/баффы/дебаффы как в спеке SR-01,
    константы из таблицы балансировки первого прохода. *[M]* — сделано:
    `public/games/seal-run-v1/core/{sim.js,balance.js}` (1D ARRIVE, ресурсный автомат
    NORMAL/EXHAUSTED/grace + хит-стан/i-frames независимыми таймерами, кинематика хищников —
    чистые функции мировой X, `SURFACE`-задел v2) + `tests/unit/seal-run-sim.unit.spec.ts`
    (11 контрактов: тайминг перехода полосы, каскад стамины, все три яруса, детерминизм-трейс)
  * [x] **SR-04** `tools/fairness-sim.mjs` + `tools/compare-variants.mjs`: headless-бот (фикс.
    политика **непрерывного Y-контроля**: держаться безопасной полосы, заходить за рыбой, если путь
    чист) по матрице устройств (как у Seal Hunter) × N сидов трассы; отчёт по разбросу дистанции/
    жизней/улова — и по девайсам, и по сидам. Гейт перед тюнингом баланса. *[M]* — сделано:
    `tools/{bot-lib,fairness-sim,compare-variants}.mjs` + `tests/unit/seal-run-bot.unit.spec.ts`.
    **Девайс-ось → профили каденса ввода** (80/160/240 мс): equal horizon убрал экранную
    геометрию из sim по построению, экранная матрица Seal Hunter здесь измеряла бы ноль —
    решение и базлайн задокументированы в `game-seal-run.md` §2.8. Для A/B тюнинги переехали
    в мутируемый `BAL` (`core/balance.js`, прецедент BAL Seal Hunter; мутирует ТОЛЬКО
    compare-variants). Базлайн: финиш-рейт 80–85%, ~82 с, каденс-разброс ср. 12.5 м.
    — **аудит 2026-07-26:** путь в формулировке неточен: харнесс лежит в
    `public/games/seal-run-v1/tools/`, а не в корневом `tools/`.
* **Фаза 2 — Core-механика + Phaser-интеграция**
  * [x] **SR-05** Vendored **Phaser 4** (≥4.1.0 «Salusa», ESM-билд; `public/games/seal-run-v1/vendor/
    phaser.esm.js`, статический файл, БЕЗ нового бандлера) + сцены Boot→Preload→MainMenu→Play→
    GameOver; Play-сцена — тонкий рендер-слой над `core/sim.js`/`core/course.js` (object pooling для
    рыбы/препятствий, Arcade Physics). Динамический `import()` вендор-бандла (~400–500 КБ gzip) ТОЛЬКО
    по нажатию «Старт». **iframe-гочи:** `allowfullscreen`; fullscreen-запрос на `pointerup` (iOS);
    guard против залипания тача при уходе iframe в фон. *[L]* → PERF — сделано (vertical slice):
    vendored **Phaser 4.2.0** ESM min (1.4 МБ), `index.html`+`style.css`+`game.js`; сцены по
    спеке §2.1 — Boot/Preload схлопнуты в create() Play-сцены (текстуры процедурные, грузить
    нечего), **MainMenu/GameOver = HTML вне canvas** (доступность); **Arcade Physics НЕ
    подключена намеренно** — коллизии авторитетны в sim-core (спека §12), спрайты позиционируются
    из состояния + `predatorPos`; пулы спрайтов; фикс-шаг аккумулятор + интерполяция; equal
    horizon через `Scale.FIT` 960×540; ввод палец/мышь/клавиши + guard залипшего тача
    (blur/visibilitychange/pointercancel); fullscreen по `pointerup`. Арт — SR-06, HUD/a11y
    полностью — SR-07, лидерборд — SR-09/10, i18n/SW — SR-11.
  * [x] **SR-06** Арт: спрайт-атлас тюленя (профиль сбоку — свободное плавание с лёгким наклоном;
    задел под будущую «галумпинг»-анимацию, не для v1) + хищники **v1 биом-точные (орка/белая акула +
    крупная акула-вариант; белый медведь/морской леопард отложены со своими биомами)** + мусор (сеть-
    призрак/пластик) + камни/водоросли/рыба; тинт из **semantic-токенов** (Foggy Coastal Utility),
    параллакс-фон (2–3 слоя). *[L]* → DESIGN — сделано: направление владельца — cartoon-flat
    underwater, тюлень Уэдделла (безухий, серебристый градиент), камни-«квази-суша» (кекуры
    пробивают ватерлинию: под водой препятствие, над — сухая макушка с пеной). Пайплайн —
    процедурные Canvas2D-текстуры (`core/theme.js` палитра/контракт + `render/art.js`),
    НЕ бинарный атлас; кадры `seal_0/seal_1` (гребок) = задел под галумпинг. Слои: статичная
    толща → далёкий 0.12× → средний 0.35× → геймплей → пена ватерлинии; reduced-motion глушит
    параллакс. Попутно исправлена честность габаритов (SR-05-плейсхолдеры меньше хитбоксов;
    инвариант — unit-тест). Детали — `game-seal-run.md` §2.10.
  * [~] **SR-07** HUD (жизни/стамина-бар/дистанция) в safe-area, HTML-инструкции и HTML-результат
    ВНЕ canvas (доступность), WCAG 2.2 AA (focus/target≥24px/`prefers-reduced-motion` глушит
    параллакс/вспышки), ярусы препятствий различимы НЕ только цветом (силуэт). *[M]* → DESIGN/EU
    — **аудит 2026-07-26:** из слайсов SR-05/06 уже есть: HUD (жизни / `role=meter` стамина / дистанция)
    с aria-подписями, HTML-меню и результат вне canvas, цели 44×44, `focus-visible`,
    `prefers-reduced-motion`. Осталось по метке в самом коде: safe-area-инсеты, полный проход WCAG и
    проверка различимости ярусов силуэтом (не только цветом).
* **Фаза 3 — Лидерборд/бэкенд**
  * [x] **SR-08** Документ `games` (`slug: seal-run`, `embed`, `how`) — без изменений схемы. *[S]*
    — сделано: запись в `gamesSeed` (RU/EN, `order: 1`, `embed: /games/seal-run-v1/index.html`);
    карточка на `/[locale]/games` и detail-iframe заработали из generic-роута без правок кода;
    инварианты сидов — существующий `seeds.int.spec.ts` (итерируется по `gamesSeed`). Попутный фикс
    оболочки: `#hud[hidden]{display:none}` — безусловный `display:flex` перебивал атрибут `hidden`,
    пустой HUD просвечивал поверх меню. Standalone-формат: прямой URL `/games/seal-run-v1/` работает
    уже сейчас; vanity-домен `sealrun.sealife.info` — отдельно SR-12 (после прода sealife.info).
  * [ ] **SR-09** Расширить `game-scores`: опциональные поля `distance`/`livesRemaining`/
    `fishCollected`/`courseSeed` (обратная совместимость с Seal Hunter — все null/omitted для
    существующих строк; `courseSeed` выводится сервером из токена SR-10, не от клиента); `score`
    остаётся required сорт-ключом (производная формула от дистанции+улова+жизней, **ДОЛЖНА оставаться
    ≤100000** — текущий Zod-кап `SubmitBody`; иначе бампнуть кап здесь же), код ранжирования в
    `leaderboard.ts` не меняется. `generate:types` + обновить `data-model.md`. *[M]*
  * [ ] **SR-10** Anti-cheat под раннер (текущий submit-хендлер `leaderboard.ts` ~L269–307 — единый
    путь с зашитым окном 50–70с), три части:
    1. **Per-game dispatch:** `SubmitBody` получает опц. поля; ветвление правдоподобия по слагу —
       Hunter оставляет фикс-окно, Run считает `minPlausibleMs = distance / MAX_SPEED_UNITS_PER_MS`;
       порог token-age (`MIN_PLAY_MS`) тоже per-game (Run допускает ~3с при ранней смерти).
    2. **Пиннинг курса в токен:** на `/start` добавить в подписанный токен `cs` (courseSeed) + сезон
       (сервер выводит `courseSeed = hash(сезон-выдачи)`, клиент не поставляет как истину); валидация
       и запись — против сезона/трассы ТОКЕНА. Чинит ложный отказ на границе ISO-недели и блокирует
       грайнд лёгкого исторического сида.
    3. **Серверная пересборка трассы** через изоморфный `core/course.js` (SR-02, тонкий адаптер в
       `src/`) → сверка `fishCollected`/`distance` с бюджетом рыбы этой трассы. Основной гейт;
       статистические капы Seal Hunter — предфильтр. Возможно ТОЛЬКО благодаря сид-детерминированной
       трассе. *[M]* → SEC
  * [ ] **SR-11** `core/alias.js`+`i18n.js`+`sw.js` для seal-run-v1 — копии структуры Seal Hunter
    (свой словарь строк, свой `CACHE`-конст, свои `localStorage`-ключи `seal_run_seed`/`_best`/
    `_lang`/`_sound`), синхронно с серверным `makeParts`/word-lists (тот же общий сервер-эндпоинт,
    только новый `game`-slug). *[S]*
* **Фаза 4 — Доступность/compliance/публикация**
  * [ ] **SR-12** Встраивание на `sealife.info` (и, если уместно тону, `sealrescue.info`) через
    существующий `/[site]/[locale]/games/[slug]` роут (канонический origin) **+ vanity-редирект 301
    `sealrun.sealife.info` → этот путь** (~5 строк Caddy/хоста; один origin → legal достижим, без
    повтора SH-10). БЕЗ отдельного standalone-домена в v1 (см. `docs/game-seal-run.md` §2.5:
    `sealife.info` ещё не в проде). Промоушен поддомена в standalone-origin — ревью ПОСЛЕ прод-запуска
    `sealife.info` и ПОСЛЕ SH-10. *[S]*
  * [ ] **SR-13** Datenschutz: добавить секцию про `localStorage`-ключи Seal Run (по аналогии с
    SH-10 п.4 для Seal Hunter) во все версии страницы — RU/EN и немецкую legal-only — т.к. игра на
    основном домене, отдельный legal-allowlist/футер НЕ нужен (страницы и так достижимы). *[S]* → EU
  * [~] **SR-14** E2E (`tests/e2e/game-seal-run.e2e.spec.ts` по конвенции `game-standalone.e2e.spec.ts`,
    без standalone-ветки) + **determinism/parity юнит-тест `generateCourse(seed)`**: golden-hash
    трассы совпадает (а) при повторе и (б) **между Node и браузерным билдом** — кросс-рантайм-свойство,
    на которое опирается серверная пересборка SR-10 п.3; CI-страж против дрейфа изоморфного модуля. *[M]* → QA
    — **аудит 2026-07-26:** половина про детерминизм уже зелена: «один сид → байт-в-байт одинаковая
    трасса» (unit). Осталось: кросс-рантайм паритет Node↔браузер (от него зависит SR-10 п.3) и сам
    e2e-смоук — файла `tests/e2e/game-seal-run.e2e.spec.ts` нет.
  * [ ] **SR-15** Аудио: SFX (подбор рыбы / удар / бафф / game-over) + опц. фоновый луп; тумблер
    звука, muted-by-default, persist в `seal_run_sound` только после явного действия пользователя. *[S]* → DESIGN
* **Фаза 5 — Polish (backlog, идеи владельца 2026-07-03)**
  * [ ] **SR-16** **Финиш как success-момент:** при достижении 900 м управление отключается,
    тюлень плавно **уплывает за правый край экрана** (анимация чисто в рендер-слое; sim уже
    FINISHED — `durationMs`/очки фиксируются в момент пересечения финиша, не анимации), короткая
    передышка — и ТОЛЬКО потом HTML-оверлей «Финиш» с итогом. UX: пережить успех до таблицы
    результата. Анти-чит/сабмит не затронуты. *[S]* → DESIGN
  * [ ] **SR-17** **«Живая» рыба:** не статичный спрайт, а медленное плавное движение по Y с
    параметрами ПО ВИДУ рыбы (мелкая — частая мелкая рябь; редкая/золотая — ленивая широкая
    синусоида). СТРОГО рендер-слой: хитбокс подбора остаётся на центре полосы (честность §1.2
    спеки), визуальная амплитуда ≤ `FISH_PICKUP_R` (спрайт не «отрывается» от хитбокса), фаза —
    детерминированная функция `atLu` (дисциплина `WIGGLE`/prey Seal Hunter, НЕ `Math.random` в
    геймплейном слое), `prefers-reduced-motion` → статично. *[S]* → DESIGN
* **Фаза 6 — Контент и прогрессия (backlog, плейтест владельца 2026-07-04)**
  * [ ] **SR-18** **Плотность + монотонно возрастающая сложность трассы.** Плейтест владельца:
    «трасса пустовата, сложность неровная — надо вначале просто, к финишу сложнее». Факты кода:
    `difficultyCeil` (course.js, спека §4.3) поднимает только ПОТОЛОК сложности 1→5 на первых
    12 000 lu (треть трассы), дальше выбор чанков равномерный по всему пулу — лёгкие чанки
    честно выпадают и у финиша, давление не растёт. Сделать: (1) difficulty-ПОЛ, растущий к
    финишу (окно [floor..ceil] ползёт вверх) и/или взвешенный выбор в пользу верхних difficulty
    на поздней дистанции; (2) поднять плотность контента — доавторить чанки/наполнение
    существующих (сохранив «дыхание» после intense §4.3, правила честности §11 и лимит
    `FISH_POINTS_BUDGET_MAX`); (3) прогнать chunk-lint + SR-04 fairness-харнесс (финиш-рейт
    80–85 % держим) и обновить спеку §4.3. Изоморфность `course.js` не трогать — трассу
    пересобирает сервер (SR-10). *[M]* → DESIGN
  * [ ] **SR-19** **Локации-биомы (для каждой: сначала описание/арт-концепт, потом реализация
    поочерёдно).** Дизайн-док на локацию (в `docs/`, каркас — арт-контракт §2.10: палитра-вариант
    в `core/theme.js`, свой набор текстур в `render/art.js`, контракт светимости сохраняется) +
    свои чанки (`biome`-тег из SR-02), своя фауна/флора и новые виды рыб/морских обитателей.
    Кандидаты владельца:
    **«Атлантида»** — затонувшие древние сооружения: колонны, арки, статуи, барельефы (декор и
    препятствия);
    **«Арктика»** — белый медведь караулит под водой И на камнях (в дополнение к орке/акуле —
    биом-точность, решение SR-01), паковый лёд/льдины сверху вместо пены, айсберги в параллаксе;
    поресёрчить атрибуты Арктики (фауна: нарвал, белуха, моржи и т.п.);
    **«Карибы»** (акватория тюленя-монаха) — тёплая светло-синяя вода, кораллы/рифы, тропическая
    флора и фауна;
    **«Антарктида»** — морской леопард (элитный тюленеед, задел SR-01) + орки, стены/огромные
    массивы льда в фоне, льдины вместо камней (в т.ч. «квази-суша» изо льда). *[L]* → DESIGN
  * [ ] **SR-20** **Мульти-раунды («уровни»): дошёл до финиша → следующий раунд в новой
    локации.** Сначала концепт-док: кривая сложности между раундами по жанровым best practices
    (раунд 1 — стандарт, дальше сложнее: скорость/плотность/новые паттерны; связка с SR-18 —
    кривая ВНУТРИ раунда — и SR-19 — локация на раунд). Недельный принцип сохраняется: трасса
    раунда k — детерминированная производная сида недели (напр. `hash(season + ':' + k)`),
    пиннится в play-токен как в SR-10 (клиент не поставляет). Статистика: число пройденных
    уровней в сабмите и лидерборде (`levelsCompleted` — расширение SR-09; ⚠️ очки за несколько
    раундов ломают гарантию `score ≤ 98 900` — пересчитать против Zod-капа 100 000 и осознанно
    бампнуть кап в том же PR, см. SR-09). Анти-чит: per-round пересборка трасс + плаузибельность
    длительности по сумме раундов (расширение SR-10); `MAX_COURSE_MS` становится per-round.
    *[L]* → DESIGN/SEC

### Реакции (без авторизации)

* [ ] **M1-T12** Эндпоинт инкремента реакций: атомарный +1, rate-limit, дедуп localStorage. Прямую запись держать за staff. *[M]* → SEC *(CLAUDE.md шаг 1)*
* [ ] **M1-T13** UI реакций (🦭 ❤️ 😂 😭). *[S]*

### SEO (база)

* [~] **M1-T15** `sitemap.xml` по языкам, `robots.txt`. *[S]* → SEO
  — **аудит 2026-07-26:** осталось только **robots.txt** — его нет нигде (нет `src/app/robots.ts`).
  Половина про per-locale sitemap уже сдана в M0-T10 и покрыта e2e (published-only, hreflang,
  x-default). Известные пропуски sitemap: rescue-centers (ждёт M2) и legal-роуты — это осознанно, не
  баг.
* [~] **M1-T16** schema.org + meta из поля `seo`. *[S]* → SEO
  — **аудит 2026-07-26:** половина «поле `seo` → meta» сделана для `content` (`[slug]/page.tsx` берёт
  `seo.metaTitle`/`metaDescription` с фолбэком). Осталось: (а) schema.org/JSON-LD — в src/ ноль
  вхождений `ld+json`; (б) решение, нужна ли группа `seo` у Species/Games/Quizzes (сейчас её нет).
* [ ] **M1-T17** SEO-кластеры (мастер-план §12) как контент-план. *[S]* → SEO

---

## M2 — Справочник спасения + Агент-1 (4–6 недель)

**DoD:** 20–50 центров с датами проверки, форма правок, rescue-квест, Агент-1 кладёт находки в очередь, кнопка «Применить предложение».

### sealrescue.info (emergency-first + витрина центров, см. DESIGN_BRIEF §5–6)

* [~] **M2-T01** «Нашёл тюленя — что делать / чего НЕ делать» (RU/EN), дистанция не хардкодится. *[M]*
  — **аудит 2026-07-26:** страница есть на RU/EN, требование «дистанция не хардкодится» выполнено.
  Осталось: демо-баннер над реальными инструкциями (`SectionShell`), нет отдельного блока «чего НЕ
  делать» (запреты растворены в 4 шагах), тексты в `src/i18n/ui.ts` вместо CMS, нет теста. ⚠️
  Содержательные пробелы — трек **BIO-**.
  — **уточнение 2026-07-26:** блок «чего НЕ делать» и тесты приехали с BIO-02/BIO-04, демо-баннер на
  `/what-to-do` снят (`demo={false}`, проверяется `rescue-advice.e2e.spec.ts`). Но та же ложная
  подпись осталась ещё на двух страницах — `/report` и `/rescue-quest` зовут `SectionShell` без
  `demo`, а дефолт `true` (см. **MOCK-03**). Тексты по-прежнему в `ui.ts`, не в CMS.
* [~] **M2-T02** Каталог центров: **list-first** + карта (toggle), фильтр-чипы с числом результатов, «штамп проверки», `socialLinks`, `tel:`, маршрут. *[L]* → DESIGN
  — коллекция `rescue-centers` в Payload уже существует с нужными полями (`location`, `socialLinks`,
  `operatingLanguages`, `verificationScore`, `verifiedByAgentAt/HumanAt`); фронтенд
  (`rescue-centers/page.tsx` и `[slug]/page.tsx`) сейчас на 100% на моках (`@/mock/sample`), не читает
  из Payload — эта задача включает и перевод фронтенда с mock на реальные данные.
  — **аудит 2026-07-26:** UI построен полностью (list-first, чипы-фильтры со счётчиками, «штамп
  проверки», `tel:`, маршрут) — не хватает только источника данных. Сверх порта mock→Payload
  отсутствуют: рендер `socialLinks` (поле в коллекции есть) и настоящая карта (в package.json нет ни
  leaflet, ни maplibre).
  — **аудит мока 2026-07-26 — грабли порта.** Эта задача теперь единственный гейт для вывода
  `sampleCenters` и для закрытия **BIO-07**. Что разъезжается между моком и коллекцией:
  (1) имена полей: у мока `city` и плоский локализованный `summary`, у коллекции города нет вовсе
  (country/region/address), а описание — локализованный richText; `agentCheckedAt`/`humanReviewedAt`
  против `verifiedByAgentAt`/`verifiedByHumanAt`;
  (2) `CenterCard.tsx` дёргает `center.phone.replace(...)` без проверки, а `phone` в коллекции
  опционален — первый же центр без телефона роняет список;
  (3) без рендера остаются `socialLinks`, `location`, `operatingLanguages`, `verificationScore`,
  `sources`;
  (4) порт обязан сохранить зелёным `frontend.e2e.spec.ts` — жёсткий HTTP 404 на неизвестный slug
  центра держится ровно на том, что у детальных роутов НЕТ `loading.tsx`; не добавлять её. Скелет
  списка тогда вешать через `<Suspense>` вокруг async-ребёнка (образец — лента главной sealife),
  иначе аварийный сайт получит белое ожидание без скелета.
* [ ] **M2-T03** Наполнить 20–50 центров (RU/EN). *[L]*
* [~] **M2-T04** Форма «Сообщить об ошибке / предложить центр» → `user-submissions` (премодерация, без email). См. **EU-10**. *[M]* → EU
  — **аудит 2026-07-26:** бэкенд готов и покрыт тестами: премодерация + field-access на `status` (аноним
  не может自-одобриться), e2e подтверждает отсутствие полей email. Осталось ровно одно: форма ничего не
  отправляет (`onSubmit` только `preventDefault`). ⚠️ В момент подключения включается риск **SEC-05** —
  публичный `create` без rate-limit.
  — **аудит мока 2026-07-26:** `_components/mock/ReportForm.tsx` — не мок формы, а **настоящая**
  форма нужной формы: плашка «email не собираем», необязательный TG/VK-хэндл, обязательное сообщение,
  `role=status` на успехе, ссылка на operator contact point в legal notice. Заглушка ровно одна —
  `onSubmit`. M2-T04 подключает отправку, а не переписывает компонент. `report-form.e2e.spec.ts` —
  единственное автоматическое доказательство в репозитории, что публичная форма не собирает email
  (инвариант №4): его ассерты перенаправить на подключённую форму, но не удалять вместе с моком.
  Вместе с подключением меняется и текст успеха («демо: ничего не отправлено») — соответствующий
  ассерт спеки правится тем же PR.
* [x] **M2-T05** Блок перелинковки sealrescue → sealife. *[S]*
  — **аудит 2026-07-26:** сделано: отдельный блок-карточка на главной sealrescue с текстом `crossLife` и
  CTA на sealife.info, строки в `ui.ts` на обеих локалях. Оговорка: блок только на главной и без теста —
  если задумывался на каждой rescue-странице, переформулировать, а не снимать галочку.
* [~] **M2-T15** 🎮 «Что делать, если нашёл тюленя?» — интерактивный rescue-квест (игра+обучение, ведёт в каталог). *[M]* → DESIGN
  — **аудит 2026-07-26:** квест работает end-to-end, включая требование «ведёт в каталог». Осталось:
  сценарии захардкожены в компоненте (не из CMS), показывается демо-баннер. ⚠️ Содержательная правка
  сценариев — трек **BIO-**.
  — **правка 2026-07-26:** «3 шага» устарело — после BIO-01/BIO-05 сценариев **пять**, и они покрыты
  `rescue-advice.e2e.spec.ts`. Демо-баннер всё ещё показывается (**MOCK-03**). Компонент лежит в
  `_components/mock/` по случайности раскладки папок: это выверенный биологом текст безопасности, его
  надо **перенести**, а не удалить вместе с моком (**MOCK-01**).

### Агент-1 (Researcher / факт-чекер)

> **Требование владельца 2026-07-26 — агенты работают С ИНТЕРНЕТОМ и перепроверяют факты.**
> Агент не является источником истины сам по себе: любое утверждение, которое он кладёт в
> `agent-proposals`, должно быть подтверждено **живой проверкой во внешних источниках** и нести
> ссылки + дату проверки + confidence. Это делает обязательными (а не опциональными) M2-T07
> (поиск + парсинг), M2-T08 (логика верификации), M2-T11 (allowlist источников по
> `Source.trustLevel`) и SEC-02/SEC-03 (sandbox + анти-prompt-injection: внешний текст — ДАННЫЕ,
> не инструкции). Приоритет источников для rescue-фактов — инвариант №8 `CLAUDE.md`
> (офиц. сайт центра → офиц. соцсети → гос./муниципальные → признанные NGO → новости).
> Провенанс проверки (`sourceVerified` / `sourceContentHash` / `lastAgentCheckedAt`) — M1-T08/EU-11.
> ⚠️ Это относится и к **контентным** агентам (M1-T06/M3-T01), а не только к rescue-фактчекеру:
> статьи о биологии тюленей пишутся с проверяемыми источниками, а не «из головы модели».

* [~] **M2-T06** Контракт вывода Агента-1: JSON-схема предложения (diff+evidence+confidence+sources) + промпт + пример. **`sources[]` обязателен и непустой; `evidence` цитирует источник дословно** (иначе предложение невалидно на уровне схемы). *[M]* *(CLAUDE.md шаг 2)*
  — **схема 2026-07-26 (PR provenance):** схемная часть контракта закрыта досрочно:
  `agent-proposals.sources` теперь `required` + `minRows: 1` — предложение без источника отклоняет
  СХЕМА, а не договорённость (инвариант №6 `CLAUDE.md`). Негативный тест проверяет, что падение именно
  на `sources`, а не на других required-полях. Осталось: JSON-схема вывода, промпт и пример.
* [ ] **M2-T07** Оркестрация (LangGraph) + **интернет-доступ агента**: поиск (Tavily/Perplexity) + парсинг (Playwright в sandbox) + снапшот страницы с хэшем. Без этого агент не имеет права утверждать факты. *[L]* → SEC
* [ ] **M2-T08** Логика проверки центров: ссылки, телефон/email/адрес, новые центры, новости; confidence + снапшот. *[L]*
* [~] **M2-T09** Запись в `agent-proposals`/`agent-runs` по API-ключу `agent`. Проверить: нет publish/delete. *[M]* → SEC
  — **аудит 2026-07-26:** половина «проверить: нет publish/delete» СДЕЛАНА и закреплена тестами (матрица
  доступа: `delete` нигде не содержит agent; `forceAgentDrafts` сажает запись агента в черновик),
  инфраструктура API-ключей включена (`useAPIKey`). Отсутствует только клиент, который реально
  аутентифицируется и пишет.
* [ ] **M2-T10** Cron + бюджет-лимит AI API + лог `cost`. *[M]* → SEC
* [ ] **M2-T11** Allowlist источников через `Source.trustLevel`. *[S]* → SEC

### Дашборд (ревью)

* [ ] **M2-T12** «Очередь изменений»: карточки, diff было→стало, Approve/Reject/Edit. *[M]*
* [ ] **M2-T13** «Применить предложение»: diff → обновить документ КАК ЧЕРНОВИК, proposal `applied`. *[M]* *(CLAUDE.md шаг 3)*
* [ ] **M2-T14** Turnstile на формы. *[S]* → SEC/EU

---

## M3 — Остальные агенты, дашборд, монетизация (6+ недель)

**DoD:** агенты 2/3/5 через очередь, дайджест, донаты, лидерборд.

### Агенты

* [ ] **M3-T01** Агент-2 (Content Admin): находки → черновики + внутренние ссылки + версии для VK/TG. *[L]*
* [ ] **M3-T02** Агент-3 (Translator integrity): **EN→RU** по glossary, stale через `content_hash`, provenance-поля. *[L]*
  — **CR-14 2026-07-26:** направление развёрнуто. Сюда же переехал вопрос о ключе глоссария:
  `source` остаётся каноническим РУССКИМ ключом TM и НЕ следует за исходной локалью контента.
  Перекладывать его на английский в рамках смены направления было бы дорого и незаметно: `source` —
  ключ upsert'а сида и `index: true`, но НЕ `unique`, поэтому перекладка создала бы 62 новых
  строки рядом с 62 осиротевшими, а тест идемпотентности остался бы зелёным (он считает только
  строки из нового списка). Плюс обратный поиск EN→RU неоднозначен уже сейчас: `haul-out` — это
  en-значение и у «лежбища», и у «залёжки». Если ключ всё же перекладывать — это delete-then-reseed
  скрипт и правка `glossaryTerms.ts`, а не переименование.
* [ ] **M3-T03** Агент-5 (SEO): title/meta/alt/hreflang/schema, каннибализация, страницы без перевода. *[M]*
* [ ] **M3-T04** Агент-6 (Security and Compliance): два раза в месяц проверяет актуальные законы, акты и нормы, требования безопасности и следует ли сайт им, нужно ли выполнять, что можно убрать, что нужно добавить или поменять. *[M]*
* [ ] **M3-T05** Daily digest в личный Telegram. *[S]*

### Дашборд (полный)

* [ ] **M3-T12** «Переводы»: RU/EN рядом, diff, статус, provenance. *[M]*
  — **аудит 2026-07-26:** переименовано из M3-T05: этот ID уже занят задачей «Daily digest в
  личный Telegram» выше. Два разных пункта под одним ID ломают рабочий процесс «ветка/коммит
  по ID» из `CLAUDE.md`.
* [ ] **M3-T06** «SEO»: страницы без meta/hreflang/canonical. *[S]*
* [ ] **M3-T07** «Модерация» UGC (лог модерации, см. EU-10). *[S]*

### Монетизация (мастер-план §12)

* [ ] **M3-T08** Донаты RU: Boosty / Telegram Stars / VK Донат / ЮKassa. *[M]*
* [ ] **M3-T09** Донаты EN/EU: Buy Me a Coffee / Ko-fi / Patreon. *[S]*
* [ ] **M3-T10** Прозрачные прогресс-бары целей. *[M]*
* [~] **M3-T11** 🎮 Лидерборд квизов/игр + бейджи + пасхалки. *[M]* — анонимно (SH-05…07); кросс-девайс идентичность (общая позиция везде) — опционально через эпик **ACC** (ACC-T10).
  — **аудит 2026-07-26:** игровая половина уже сдана в SH-05..07 (анонимная доска, недельный сезон, без
  PII, анти-чит + e2e). Не существует: подсчёт очков квизов (в `Quizzes` нет полей результата), бейджи,
  пасхалки.

> Появятся донаты/платные продукты → нужны Terms/AGB + (для цифровых товаров) Widerrufsrecht; платёжные рельсы для RU проверить перед запуском.

---

## M4 — Масштаб

* [ ] **M4-T01** Telegram-бот / mini-app: квиз, донат, платные квесты. *[L]*
* [ ] **M4-T02** Платные цифровые продукты (PDF, карточки, материалы учителям). *[M]*
* [ ] **M4-T03** Print-on-demand мерч (Printful/Printify). *[M]*
* [ ] **M4-T04** Полноценная карта центров. *[M]*
* [ ] **M4-T05** 🎮 idle «собери лежбище» (Phaser-игра — «Seal Run» уже расписана отдельно, см. **SR-01..SR-15**). *[L]*
* [ ] **M4-T06** Поиск по сайту (Meilisearch/Typesense); позже pgvector/RAG. *[L]*
* [ ] **M4-T07** Агент-4 (SysAdmin) на полную: uptime, broken links, Lighthouse, очереди, алерты. *[M]* → SEC
* [ ] **M4-T08** Конфиг ниши (`Niche`) для клонирования на другие темы. *[L]*

---

## ACC — Публичные аккаунты / кросс-скоуп идентичность (опционально, пост-MVP)

> **Статус: не в MVP.** Включается только когда понадобится кросс-девайс идентичность: общая позиция
> в лидербордах, ачивки, кастомные тюлени/hero-SVG, новые локации/прогресс в играх. Правовой режим и
> обоснования — `COMPLIANCE_EU_DE.md` §9. **Ключ:** основание аккаунта — **договор** (DSGVO Art. 6(1)(b)),
> НЕ согласие; согласие нужно только под newsletter. Аккаунт всегда **опционален** — аноним продолжает
> играть (SH-05…07). Память: [[accounts-contract-vs-consent]].

**Кросс-доменный SSO (почему так):** cookie не шарится между разными корневыми доменами
(`sealife.info` ↔ `sealrescue.info` — а их у проекта ровно два, см. доменную политику выше),
поэтому «один username везде» = центральный IdP.

```
                       ┌──────────────────────────────────────────────┐
                       │      accounts.sealife.info  (central IdP)      │
                       │   ─ единая таблица `players` (source of truth) │
                       │   ─ magic-link / passkeys / (опц.) пароль      │
                       │   ─ выдаёт сессию (cookie) + OIDC-токены        │
                       └───────────────┬──────────────────┬────────────┘
        cookie  .sealife.info          │                  │   OIDC (Auth Code + PKCE)
   (общая для всех поддоменов)         │                  │   redirect-login + токен
        ┌──────────────┬──────────────┘                   │
        ▼              ▼                                  ▼
  sealife.info   game.sealife.info               sealrescue.info
  (тот же root —  quizzes.sealife.info           (другой root —
  cookie работает) sealrun.sealife.info           OIDC redirect)
                   (все игры/квизы — сюда,
                    cookie бесплатно)
```

> Правило: что можно — заводим как `*.sealife.info` (cookie бесплатно); отдельных игровых/бренд-доменов
> проект не заводит (доменная политика: только `sealife.info` и `sealrescue.info` + их поддомены).
> Второй корень, `sealrescue.info`, — через redirect-SSO. Username уникален глобально через `UNIQUE`
> на `players`.

### Фаза 0 — сейчас (без аккаунтов)

* [x] **ACC-T00** Анонимные device-bound идентификаторы (лидерборд SH-05/06/07). *(текущее состояние)*

### Фаза 1 — минимальные аккаунты

* [ ] **ACC-T01** Коллекция `players` (Payload `auth`, **отдельная** от admin `users`) — схема ниже (b). *[M]* → SEC/EU
* [ ] **ACC-T02** Аутентификация = **magic-link only** (без паролей → без reset-флоу и хранения секрета): одноразовый токен (короткий TTL, single-use) + email-верификация. Сессия — httpOnly/Secure/SameSite cookie на `.sealife.info`; rate-limit на login и выдачу ссылки; защита от email-enumeration. *[L]* → SEC/EU
* [ ] **ACC-T03** Кросс-доменный SSO: cookie для `*.sealife.info`; **OIDC (Auth Code + PKCE)** для sealrescue.info и игровых доменов (`accounts.sealife.info` как IdP). *[L]* → SEC
* [ ] **ACC-T04** Self-service права субъекта: экспорт (JSON, Art. 15/20), правка username/email (Art. 16), **удаление аккаунта (Art. 17)** с анонимизацией публичного вклада (username → «удалённый тюлень»; строки лидерборда/комментариев не удаляются). *[M]* → EU
* [ ] **ACC-T05** Миграция анонима: при первом логине с устройства — «забрать очки этого устройства» (merge anon-id **текущего** устройства, с подтверждением; без авто-merge между устройствами). *[M]* → SEC
* [ ] **ACC-T06** Возрастной гейт: самодекларация **«16+»** (хранить boolean, не дату; Art. 8 DSGVO + §§104–113 BGB; согласуется с EU-05). *[S]* → EU
* [ ] **ACC-T07** Обновить Datenschutz (RU/EN + немецкая legal-only версия, если она доживёт до аккаунтов — **EU-06**), Terms (становятся обязательными), реестр Art. 30, DPA email-провайдера (EU: Brevo/Mailjet/Scaleway TEM/self-host Postfix). *[M]* → EU

### Фаза 2 — расширение

* [ ] **ACC-T08** Passkeys (WebAuthn) + опциональный пароль (**Argon2id**, соль; никогда plaintext). *[M]* → SEC
* [ ] **ACC-T09** Newsletter: отдельный неотмеченный opt-in + **Double-Opt-In** (DE/UWG) + таблица `consents` (тип, версия текста, время, метод; Art. 7(1)); отзыв так же прост, как согласие (Art. 7(3)). *[M]* → EU
* [ ] **ACC-T10** Привязать к аккаунту фичи **на основании-договора** (без нового согласия): кросс-девайс лидерборд (поверх анонимного SH-07 / M3-T11), ачивки/бейджи, кастомные тюлени/hero-SVG, прогресс по локациям. *[L]* → DESIGN

### (b) Схема коллекции `players` + access control

> **Отдельная** от admin `users` (Payload поддерживает `auth` на нескольких коллекциях): публичные
> пользователи НЕ получают доступ в админку. Поля минимальны (COMPLIANCE §9.4).

```
players (auth: true)
  id             uuid     — внутренний ключ; единственное, по чему джойним
  email          email    — auth/reset/security (рассмотреть encrypt-at-rest)
  emailVerified  date
  passwordHash   text     — NULL при magic-link-only
  username       text     — публичный, ГЛОБАЛЬНО уникальный (citext / UNIQUE); ЕДИНСТВЕННОЕ публичное поле
  locale         select   — ru | en
  ageConfirmed   checkbox — «16+» boolean (НЕ дата рождения)
  lastLoginAt    date
  createdAt / updatedAt
  — согласия: ОТДЕЛЬНАЯ коллекция `consents` (player, type, textVersion, grantedAt, method)
НЕ хранить: имя, адрес, телефон, дату рождения, пол.
```

**Access (явный, как у всех коллекций):** `read` — себя; публично доступен ТОЛЬКО `username` через
санитизированный endpoint (никогда email); `create` — публичный (через auth-флоу регистрации);
`update` — только своя запись; `delete` — только своя запись, **НИКОГДА `agent`**. Поля `email`/`passwordHash`
скрыты от чужого read. Публичные джойны (лидерборд/комментарии) — только по `id` → `username`.
**После изменения схемы:** `npm run generate:types`; отразить в `data-model.md` (`players`, `consents`).

### (c) Копирайтинг регистрации + тексты согласий (RU/EN)

> Цель: **один** обязательный контрол (Terms + Политика конфиденциальности) + **один** отдельный
> opt-in (newsletter). Никаких преднастроенных галочек, никакого бандлинга (Art. 7(4)). Финальные
> формулировки — на юр-сверку (EU-06). Немецких формулировок нет: интерфейс существует только на
> RU/EN, немецкими остаются лишь сами legal-документы под `/de`.

* [ ] **ACC-T11** Тексты экрана регистрации и согласий, локализованные RU/EN:
  * **Обязательный чек-бокс (Terms + Политика конфиденциальности, НЕ предотмечен):**
    * RU: «Я принимаю [Условия использования] и ознакомился(-ась) с [Политикой конфиденциальности].»
    * EN: «I accept the [Terms of Use] and have read the [Privacy Policy].»
  * **Опциональный newsletter-opt-in (отдельный, НЕ предотмечен; Double-Opt-In):**
    * RU: «Присылайте мне новости SeaLife на email. Отписаться можно в любой момент.»
    * EN: «Send me occasional SeaLife news by email. You can unsubscribe anytime.»
  * **Age-gate (16+):**
    * RU: «Мне 16 лет или больше.» · EN: «I am 16 or older.»
  * **Verification email:** короткий single-use link; текст «подтвердите, что адрес ваш»; TTL в минутах.
    Логировать версию текста согласия (Art. 7(1)).
  * *[M]* → EU/DESIGN

---

## Сквозные треки (непрерывно)

### SEC — Безопасность (OWASP LLM + web)

* [~] **SEC-01** Отдельный API-ключ на агента; ротация. *[S]*
  — **аудит 2026-07-26:** механизм персональных API-ключей включён в схеме (`auth.useAPIKey` + роль
  `agent`). Отсутствуют выдача ключа агенту и процедура ротации; аккаунта агента нет ни в одном сиде.
* [ ] **SEC-02** Sandbox для парсинга внешних страниц. *[M]*
* [ ] **SEC-03** Анти-prompt-injection: внешний контент = данные, не инструкции. *[M]*
* [ ] **SEC-04** Audit log действий агентов. *[S]*
* [~] **SEC-05** Rate-limiting форм и API. *[S]* — `user-submissions.create` сейчас публичный
  (`() => true`) БЕЗ rate-limit/CAPTCHA, в отличие от лидерборда (HMAC+Zod+rate-limit); реальный
  вектор спама модерационной очереди до **M2-T14** (Turnstile). Приоритизировать перед M2-T04 идёт в прод.
  — **аудит 2026-07-26:** API-половина сделана: лидерборд лимитирует 30 запросов/60 с и отдаёт 429
  (покрыто int-тестом). Не закрыт публичный путь форм: `user-submissions.create` — по-прежнему `() =>
  true` без rate-limit и CAPTCHA.
* [x] **SEC-06** Проверка: только admin/editor имеют `delete`; `agent` не публикует. *[S]* —
  закреплено автотестом (QA-13, 2026-07-02): `tests/int/access-matrix.int.spec.ts` гоняет всю
  матрицу и отдельными ассертами проверяет «delete нигде не содержит agent» +
  `forceAgentDrafts` (create/update агентом с `_status: published` сохраняется draft'ом).
* [x] **SEC-07** Явный access (create/update/delete) для коллекции `media`. *[S]* — сделано в
  QA-13 (2026-07-02): inline `Media` в `payload.config.ts` получил `create/update/delete: isEditor`
  (до этого Payload-дефолт пускал ЛЮБОГО залогиненного, включая agent); мёртвый
  `src/collections/Media.ts` удалён; матрица в `data-model.md` обновлена и закреплена тестом.

### EU — Compliance (GDPR / TDDDG / DDG / BFSG / DSA / AI Act) — см. `COMPLIANCE_EU_DE.md`

* [ ] **EU-01** Аудит собираемых персональных данных (цель — почти ноль). *[S]*
* [ ] **EU-02** Процедура «право на удаление/доступ». *[S]*
* [x] **EU-03** Маркировка AI-контента видна пользователю (база; полная схема — EU-11). *[S]*
  — **аудит 2026-07-26:** базовая пользовательская метка есть и рендерится (`aiGenerated` + `AiBadge` на
  детали контента и вида). Осталось: тест (в tests/ ноль упоминаний) и покрытие rescue-данных.
  — **закрыто 2026-07-26 (PR EU-11 + biology):** метка покрыта тестом
  (`tests/unit/ai-badge.unit.spec.ts`, 11 кейсов) и переведена на полную шкалу — база выполнена.
  Rescue-данные остаются за EU-11 / M2-T02, там их место.
* [~] **EU-04** **WCAG 2.2 AA:** контраст, alt, клавиатура, семантика, видимый focus, target size, reduced motion. *[M]* → DESIGN
  — **аудит 2026-07-26:** фундамент в дизайн-системе: `:focus-visible`, `prefers-reduced-motion`,
  обязательный `alt` у media, sr-only подписи. Не хватает **верификации**: axe не подключён (QA-26),
  аудита нет, системного правила target-size по CSS сайта нет.
* [ ] **EU-05** Детские разделы: без форм с PII; без поведенческой рекламы на детей. *[S]*
* [ ] **EU-06** Перед EN/EU-запуском — сверка с юристом (IT-/Datenschutzrecht). **Отдельный вопрос
  юристу (после снятия DE как языка сайта 2026-07-26):** нужны ли немецкоязычные legal-страницы
  (`/de/legal-notice`, `/de/privacy`, `/de/cookies`, `/de/terms`) вообще, если сайт говорит только
  на RU/EN, а оператор сидит в Германии — сейчас они сохранены осознанно, «на всякий случай»,
  и это НЕ решённый вопрос. *[S]*
* [~] **EU-07** **DDG Impressum / Anbieterkennzeichnung:** legal-notice доступен с каждой страницы; RU/EN текст + немецкая версия под `/de` (legal-only локаль); контактный email; ответственное лицо/legal entity + адрес; для новостей — «§18 MStV»-ответственный; без скрытых legal-ссылок; OS-Plattform-ссылку не ставить. Обязанность идёт от юрисдикции оператора (Германия), а не от языков сайта; объём немецкой части — на **EU-06**. *[M]*
  — **аудит 2026-07-26:** **ЮРИДИЧЕСКИ БЛОКИРУЮЩЕЕ.** Структура сделана и закреплена e2e: legal-ссылки в
  футере каждой страницы, заголовки §5 DDG и §18 Abs. 2 MStV. Не хватает единственного — реальных данных
  оператора: в `src/site/legal.ts` до сих пор `[Имя или название]`, `[Улица, дом]`,
  `[email@example.org]` (и то же в немецком тексте). Это часы своих данных, не недели кода.
* [~] **EU-08** **§25 TDDDG consent:** non-essential cookies/storage только после opt-in; «Reject all» так же доступен, как «Accept all»; отзыв так же прост, как согласие; Cookie-Settings из footer; CMP логирует согласия. *[M]*
  — **аудит 2026-07-26:** реализовано по существу и закреплено e2e (загрузка Plausible только после
  opt-in — проверяется по сети; равнозначность кнопок — по вычисленным стилям; отзыв; пустой
  localStorage). Единственный пробел — **лог согласий** (сервер ничего не пишет). NB: M0-T11 стоит `[x]`
  за ту же реализацию — рассинхрон бухгалтерии.
* [ ] **EU-09** **AVV/DPA-реестр** (= M0-T14): hosting · БД · email · аналитика · AI-провайдеры · платежи · CDN/хранилище; трансферы в третьи страны + гарантии. *[S]*
* [~] **EU-10** **UGC / DSA readiness:** все submissions премодерируются; abuse/report-канал (notice-and-action); лог модерации; нет публичного UGC без ревью; appeals-процесс, если появятся публичные аккаунты/комментарии (см. эпик **ACC**). *[M]*
  — **аудит 2026-07-26:** премодерация обеспечена и закреплена тестом (field-access на `status` не даёт
  анониму самоодобриться). Отсутствуют: рабочий канал notice-and-action (форма ничего не шлёт), лог
  модерации, процесс апелляций.
* [~] **EU-11** **AI Act transparency (Art. 50, к 08.2026):** метки AI-assisted / AI-translated / AI-checked / Human reviewed / Source verified; статус проверки человеком; disclosure, если появится чат-бот; маркировка дипфейков. *[M]*
  — **аудит 2026-07-26:** **на критическом пути.** Смена контент-стратегии 2026-07-26 (агент пишет
  черновик → человек ревьюит → публикация) не может дать обещанную provenance-гарантию без этих полей.
  Сегодня существует ровно один булев `aiGenerated`; ни одного из
  aiTranslated/aiChecked/humanReviewed/reviewedBy/reviewedAt/sourceVerified/sourceContentHash/lastAgentCheckedAt/lastHumanVerifiedAt
  нет.
  — **схема 2026-07-26 (PR provenance):** поля шкалы существуют и покрыты
  `tests/int/provenance.int.spec.ts`.
  — **UI 2026-07-26 (PR EU-11 + biology):** `AiBadge` рендерит шкалу целиком — ряд меток
  AI-assisted / AI-translated / AI-checked / Source verified / Human reviewed (+ дата ревью,
  штамп-иконка), пустая группа = ничего не рендерим. Выбор меток — чистая функция
  `provenanceMarks()` в `src/content/provenance.ts` (вне `src/app/**`, поэтому считается в пороге
  покрытия), 11 unit-тестов; подключено к `ContentDetail`, странице вида и `SampleDetail`.
  Флаги локализованы, поэтому ru показывает «Черновик готовил AI», а en того же материала —
  «Machine translation». Легаси-`aiGenerated` остаётся запасным вариантом, пока группа пуста.
  Осталось: `VerificationStamp` для `rescue-centers` всё ещё кормится моками (M2-T02) и
  disclosure чат-бота (его нет).

### BIO — Предметная точность (морская биология и спасательные инструкции)

> Первый **содержательный** аудит проекта (2026-07-26): не код, а фактура. Проверялись сеяные виды
> и статьи, инструкции «нашёл тюленя», глоссарий, модель `rescue-centers` и биология игр.
> Полный разбор с цитатами — в описаниях задач ниже.
>
> **Почему это трек, а не «потом».** Сид биологии после отмены M1-T06 становится few-shot-образцом,
> по которому агент будет писать статьи (M2-T06) — а до BIO-16 он вдобавок писался в БД сразу как
> `published` (`src/seed/lib.ts`; теперь `draft`). Ошибку уровня семейства дешевле убрать сейчас, чем после сотни
> сгенерированных текстов. Инструкции же — **публичная безопасность**: их правит копирайт, а не код.
>
> Публичного запуска пока нет (разработка локальная), поэтому «safety» здесь означает
> «блокирует публикацию sealrescue», а не «прямо сейчас кому-то вредит».

**Безопасность спасательных инструкций** (правится в `src/i18n/ui.ts` + `RescueQuest.tsx`):

* [x] **BIO-01** Добавить главное, чего нет вообще: **тюлень на берегу — обычно норма, а одинокий
  детёныш чаще всего НЕ брошен**; наблюдать издалека и не вмешиваться. Сейчас четыре шага идут от
  «не подходите» сразу к CTA «найти центры». Матери обыкновенного тюленя оставляют детёнышей на
  берегу на часы, серые тюленята проводят на суше ~3 недели. Изъятие здоровых бельков
  доброжелательными прохожими — главный реальный вред. Ставить **первой строкой**, до нумерованных
  шагов, на обеих локалях. *[S]*
  — **сделано 2026-07-26 (PR EU-11 + biology):** блок «скорее всего, с ним всё в порядке…
  одинокий детёныш обычно НЕ брошен» стоит первым, до нумерованных шагов, на ru/en
  (`rescueNormal` в `src/i18n/ui.ts`); порядок блоков заперт e2e-тестом.
* [x] **BIO-02** Добавить запрет **«никогда не сталкивайте и не относите тюленя в воду»** — самая
  частая летальная ошибка очевидцев. Он есть в `DESIGN_BRIEF` и в необязательном квесте, но НЕ в
  рендерящихся шагах. Туда же: не поливать водой, не накрывать, не перевозить самостоятельно. *[S]*
  — **сделано 2026-07-26:** блок «Чего НЕ делать никогда» с четырьмя запретами (в воду,
  поливать/накрывать, кормить/поить, перевозить самому) — в рендерящихся шагах, не только в квесте.
* [x] **BIO-03** Добавить предупреждение об **укусах и зоонозах**: тюлень кусает быстро и сильно;
  укус и контакт передают инфекции («палец тюленя» / микоплазмоз, бруцелла, лептоспира), опасные и
  для собак. Сейчас в репозитории ноль пользовательских упоминаний. Это объясняет, зачем убирать
  собак, и защищает самого читателя. Тон — спокойный, без нагнетания. *[S]*
  — **сделано 2026-07-26:** предупреждение об укусах и зоонозах («палец тюленя», микоплазмоз)
  сразу после запретов — объясняет, зачем убирать собак, и защищает читателя.
* [x] **BIO-04** Заменить пустое «оцените состояние с расстояния» списком **наблюдаемых признаков**:
  видимая рана или кровь; запутанность в сети/леске/пластике; тяжёлое или шумное дыхание, кашель;
  выделения и корки вокруг глаз/носа; заметная худоба (рёбра, таз, складки на шее); нет реакции на
  обычный шум пляжа; детёныш в белом эмбриональном мехе один; не отпала пуповина. *[S]*
  — **сделано 2026-07-26:** вместо «оцените состояние» — семь наблюдаемых признаков
  (`rescueSign1`…`rescueSign7`), каждый проверяется e2e.
* [x] **BIO-05** Разделить сценарий квеста «тюлень выглядит вялым»: сейчас правильным ответом
  помечено «наблюдаем издалека», но **вялость и отсутствие реакции — повод звонить**, а не
  успокаиваться. Нужны два разных сценария: «лежит и спит» (не вмешиваться) и «не реагирует,
  тяжело дышит, есть рана» (звонить). *[S]*
  — **сделано 2026-07-26:** `RescueQuest.tsx` переписан на пять сценариев; «лежит и спит»
  (не вмешиваться) и «рана + тяжёлое дыхание + нет реакции» (звонить) разведены, «забрать
  детёныша» помечен как самая частая ошибка.
* [x] **BIO-06** Дать **нижнюю границу дистанции**. Решение «не хардкодить одно универсальное число»
  правильное юридически, но человеку на пляже сейчас не дано ничего. Формулировка: «держитесь не
  ближе 100 м — а где местные правила требуют больше, соблюдайте их». Мировые ориентиры: NOAA 45 м,
  BDMLR и Seal Rescue Ireland ~100 м, Ваттовое море до 300 м. *[S]*
  — **сделано 2026-07-26:** «не ближе 100 метров — а где местные правила требуют больше,
  соблюдайте их»; числовой порог есть, юридическая оговорка сохранена.
* [~] **BIO-07** Аварийный CTA ведёт в **моки**: «Найти центры рядом» открывает каталог, где два
  выдуманных центра с правдоподобными телефонами стоят рядом с одним настоящим (реальная станция,
  реальный номер). В настоящей аварии фейковый и настоящий контакт визуально неразличимы — прямое
  нарушение инварианта №7. До M2-T02 либо убрать CTA и честно сказать, что каталог не готов, либо
  сделать демо-пометку невозможной для пропуска. *[S]* → блокирует публикацию sealrescue
  — **частично 2026-07-26:** настоящая станция с рабочим телефоном из `src/mock/sample.ts`
  убрана — все четыре карточки теперь вымышленные («(sample)», example.org, заведомо нерабочие
  номера), демо-плашка на каталоге осталась. Неразличимость фейкового и настоящего контакта
  устранена; сам каталог всё ещё мок — закрывается M2-T02.

**Фактические ошибки в контенте сида** (после BIO-16 сид пишется как `draft`, а не `published`):

* [x] **BIO-08** `how-seals-sleep` приписывает **однополушарный сон настоящим тюленям**. Это
  особенность **ушастых** (морские котики, сивучи), связанная со сном в воде с гребущим ластом.
  Фоциды — а это все три вида в сиде и весь бренд проекта — спят **двуполушарно**, в том числе на
  дне и в дрейфовых погружениях на задержке дыхания. Ошибка уровня семейства; статья вдобавок не
  помечена `aiGenerated`, в отличие от корректной соседней. *[S]*
  — **сделано 2026-07-26:** однополушарный сон в `how-seals-sleep` переписан на ушастых;
  фоциды спят двуполушарно (в т.ч. на дне и в дрейфовых погружениях).
* [x] **BIO-09** Статус балтийской нерпы **противоречит сам себе**: сид сеет `Pusa hispida botnica`
  как VU, а глоссарий утверждает, что `Pusa hispida` = LC. Сайт публикует два статуса для одного
  животного, и нигде не указано, что VU — оценка **подвида**, а не вида. Нужны структурные поля
  (`assessmentScope` / год / ссылка на Red List) и рендер вида «VU — оценка балтийского подвида;
  вид в целом LC». Ареал «Балтийское море» тоже шире реального. *[S]* → зависит от BIO-13
  — **сделано 2026-07-26:** сид пишет `conservationAssessment` со `scope: subspecies`,
  оценённой единицей и годом; ареал сужен до реального.
* [x] **BIO-10** Четыре неверных кода IUCN в глоссарии: гренландский тюлень NT→**LC** (~7–8 млн
  особей); хохлач EN→**VU**; морской заяц NT→**LC** (запомнившийся Threatened — это US ESA DPS,
  другой инструмент); гавайский тюлень-монах VU→**EN** (~1600 особей — занижение особенно опасно
  для природоохранного сайта). Остальные ~30 кодов корректны. *[S]*
  — **сделано 2026-07-26:** четыре кода в `src/seed/glossaryTerms.ts` исправлены
  (гренландский LC, хохлач VU, морской заяц LC + пометка про US ESA DPS, гавайский монах EN).
* [x] **BIO-11** Карточка серого тюленя: этимология названа **латынью**, тогда как *Halichoerus
  grypus* — **греческий** («морская свинья с крючковатым носом»); русский и английский тексты вдобавок
  противоречат друг другу. Размер дан одним числом «≈ 2 м» прямо над фактом «самцы заметно крупнее
  самок» — заменить на диапазон по полу (самки ≈ 1.8–2.0 м, самцы ≈ 2.2–2.6 м). *[S]*
  — **сделано 2026-07-26:** этимология названа греческой в обеих локалях; размер — диапазон
  по полу вместо одного числа.
* [x] **BIO-12** Терминология haul-out: в опубликованной новости стоит английское **rookery** и
  русское **лежбище** про залёжку балтийских фоцид. В английском rookery — про ушастых и птиц; для
  настоящих тюленей это haul-out. В русском аналогично: правильное слово — **залёжка**, и его нет в
  глоссарии вообще. Глоссарий半-знает это (у «лежбища» стоит контекстная пометка), а сид выбрал
  неверную ветку. Правило понадобится агенту-переводчику (M3-T02). *[S]*

  — **сделано 2026-07-26:** haul-out / залёжка вместо rookery / лежбища в сиде.
**Схема, без которой предметные ошибки нельзя ни проверить, ни переспросить:**

* [x] **BIO-13** Структурные поля статуса вместо свободного текста: `code` + `assessmentScope`
  (вид / подвид / субпопуляция / региональная оценка) + `assessmentYear` + ссылка на Red List — в
  `Species` и `Glossary`. Сейчас статус живёт в текстовой заметке и в голом select без года и
  ссылки, поэтому **интернет-агент физически не может его перепроверить**, а расхождения вроде
  BIO-09 живут незамеченными. *[S]* → предпосылка для M2-T08
  — **схема 2026-07-26 (PR provenance):** сделано: группа `conservationAssessment`
  (`src/fields/conservation.ts`) на `species` — `scope` (вид/подвид/субпопуляция/региональная),
  `assessedEntity`, `assessmentYear`, `sourceUrl`, `listingSystem` (IUCN/HELCOM/национальная/US ESA).
  Теперь «VU у балтийского подвида» отличимо от «LC у вида», а агент может сверить статус по ссылке.
  Перенос статусов из заметок глоссария в структурные поля — данными, вместе с BIO-10.
* [x] **BIO-14** Связь `sources` у `Content`, `Species` и `Glossary` (сейчас есть только у
  `rescue-centers`). Агенту, который ходит в интернет, **некуда записать, что он прочитал**, а
  читателю негде это увидеть. *[S]* → идёт вместе с M1-T08/EU-11
  — **схема 2026-07-26 (PR provenance):** сделано: связь `sources` добавлена `content`, `species` и
  `glossary` (`sourcesField()`). Раньше она была только у `rescue-centers` — агенту, который ходит в
  интернет, было некуда записать прочитанное.
* [ ] **BIO-15** Модель `rescue-centers` не выражает фактов, важных в аварии: один `phone` (у
  станций отдельная круглосуточная линия — из-за этого фильтр «есть экстренный контакт» из брифа
  невыполним), нет часов работы, нет зоны обслуживания (список по расстоянию отправит к станции,
  которая не отвечает на этом побережье), нет флага «не везите животное — мы приезжаем сами»
  (стандартная инструкция большинства станций, противоположная тому, что подразумевает каталог),
  нельзя отличить государственного ответчика от NGO; `status` смешивает «работает ли центр» и «жива
  ли ссылка»; один `verificationScore` на запись не даёт выполнить инвариант №8 по полям. *[M]*
* [x] **BIO-16** Прекратить сеять непроверенную биологию как `published` и промаркировать
  AI-происхождение: `src/seed/lib.ts` пишет `content`/`species` со статусом `published`, у видов нет
  ни `sources`, ни `aiGenerated`, а из статей флаг стоит только у одной — в результате бейдж AI
  висит на корректной статье и отсутствует у двух ошибочных. Либо перевести сид в черновики, либо
  провести через ревью, определённое в M1-T08/EU-11, и использовать исправленные записи как
  few-shot-примеры для M2-T06. *[S]*

  — **сделано 2026-07-26:** `SEED_STATUS_CONTENT = 'draft'` в `src/seed/lib.ts` (games
  сознательно остаются published — иначе лидерборд не резолвит slug); провенанс проставляется
  по локалям: ru — `aiAssisted`, en — `aiTranslated`.
**Игры:**

* [ ] **BIO-17** Герой Seal Run — **тюлень Уэдделла** (антарктический паковый лёд, самое южное
  млекопитающее) — плывёт по умеренной прибрежной трассе с ламинарией и **белыми акулами**, которых
  он в природе не встретит никогда. Это самоподрыв: дизайн-док специально настаивает на
  биом-точности **хищников** и тут же ставит антарктического тюленя в умеренный ростер. Решение:
  переименовать героя v1 в **серого тюленя** — серебристо-серый контршейдинг с тёмными пятнами,
  крупные глаза и безухий силуэт уже нарисованы и подходят точно, переделка арта не нужна; это
  правильный вид для умеренного побережья и заодно бренд-вид Балтики/Северного моря. Название
  «Уэдделла» приберечь для отложенного антарктического биома, где он — добыча морского леопарда. *[S]*
* [x] **BIO-18** ✅ Зафиксировано как **верное**, чтобы не «исправили» по ошибке: статья про вибриссы
  корректна и аккуратно хеджирована; ростер хищников Seal Run биом-точен осознанно (орка + белая
  акула для прибрежного биома; белый медведь отложен в арктический, морской леопард — в
  антарктический, с письменным обоснованием и биом-тегом чанка, который проверяет линт); сети-призраки
  и пластик смоделированы как помеха-удушение, а не как атакующий враг — это и биологически верно
  (запутывание в брошенных снастях — ведущая антропогенная причина травм), и по миссии; локомоция
  фоцид (тяга задними ластами, галумпинг на суше) верна. *[S]*

### MOCK — Вывод дизайн-мока из эксплуатации

> Решение владельца 2026-07-26: **M0-T19 выполнен**, дальше сайты наполняются реальным контентом, и
> sample-данные больше не нужны. Аудит мок-слоя (7 агентов, 2 адверсариальных ревьюера) показал, почему
> это не одно действие «удалить папку», а очередь из четырёх:
>
> **Папка врёт про своё содержимое.** `src/app/(frontend)/_components/mock/` — не демо-слой рядом с
> настоящим фронтендом. Это текущая реализация общего route-guard'а разделов (`mockSection.tsx`, **13**
> route-импортёров, среди них все пять УЖЕ payload-backed), fallback реального медиа
> (`content/Cover.tsx` → `PlaceholderMedia`) и боевой error boundary (`error.tsx` → `ErrorBlock`).
> `rm -rf` по ней роняет сборку **всего** публичного фронтенда, а не «демо-страниц».
>
> **Наполнение контентом выводит мок только на sealife.** Там пять из шести разделов nav уже читают
> Payload. На sealrescue написание текстов не разблокирует ничего: три из четырёх пунктов nav — мок или
> заглушка. `/rescue-centers` не имеет **ни одного** читателя из Payload при полностью готовой
> коллекции; `/rescue-news` не имеет модели данных вообще; `/report` рисует настоящую
> GDPR-форму, чей `onSubmit` только `preventDefault()`.

* [ ] **MOCK-01** Вынести production-код из `_components/mock/` — переезд без изменения поведения. *[M]*
  Это предусловие всех остальных строк трека и разница между «уборкой» и «падением сборки».
  Переезжают: `PlaceholderMedia` → `_components/ui/`, `ErrorBlock` → `_components/ui/`,
  `RescueQuest`/`ReportForm`/`CenterCard`/`MapToggle` → `_components/rescue/`.
  ⚠️ `mockSection.tsx` — **не** чистое переименование: помимо guard'ов (`requireSection`/
  `sectionMetadata`/`requireDetail`/`detailMetadata` + типы Next 15 async-params) он реэкспортит
  `parseState` и экспортит `SectionListView`, который рендерит демо-`StateSwitcher` и `MockList`
  (импортируется `quizzes` и `rescue-news`). Сначала расщепить на production- и demo-часть, потом
  двигать — иначе демо-свитчер переедет в «production»-папку.
  ⚠️ `MockBanner` **не** удалять: он импортируется напрямую детальной страницей центра, и до
  **M2-T02** это load-bearing safety UI (инвариант №7), а не украшение.
  Свободная победа тем же PR: `sampleArticles`/`findArticle`, `sampleNews`/`findNews`,
  `sampleMemes`, `sampleSpecies`/`findSpecies` и типы `SampleMeme`/`SampleSpecies` — **ноль**
  импортёров (эти разделы уже переехали на Payload), удаляются сегодня без последствий.
  DoD включает доки, которые указывают на старые пути: `docs/architecture.md`, `docs/data-model.md`,
  докблок `_components/ui/VerificationStamp.tsx`, `DESIGN_BRIEF §11` (целая нормативная глава про
  «design mock mode»; два URL в ней уже сегодня неверны — детали статей живут на `/[locale]/<slug>`,
  роутов `articles/[slug]`/`news/[slug]` не существует), `localization.md`, `local-development.md`.

* [ ] **MOCK-02** Перенести переиспользуемые ключи `src/mock/copy.ts` в `src/i18n/ui.ts`. *[M]*
  ~30 строк UI живут только в мок-словаре: error/retry/loading, весь набор локатора (`filters`,
  `resultsCount`, `mapToggle`, `mapPlaceholder`, `call`, `website`, `route`, `reportIssue`,
  `distanceUnknown`), весь набор формы и весь набор квеста. Умирают вместе с моком только
  `mockNotice` и `state*`.
  ⚠️ Оценка **не** `[S]`, и это не «переименование»: `ui.ts` хранит плоский
  `Record<Locale, Record<UIKey, string>>`, а `t()` возвращает `string` — тогда как `resultsCount`
  это `(n: number) => string`, а `empty` и `error` — `Record<SiteId, string>` (тон различается по
  сайту). Либо расширять тип словаря, либо расплющивать в per-site ключи (прецедент в `ui.ts` —
  `notFoundBodyLife`/`notFoundBodyRescue`).
  ⚠️ `filterAll` уже есть в `ui.ts` с побайтово теми же значениями — не продублировать.
  ⚠️ `form*` — GDPR-нагруженные строки, дословно проверяются `report-form.e2e.spec.ts`.
  ⚠️ При схлопывании `EmptyState` (мок) и инлайнового empty-блока `content/ContentList.tsx` в один
  компонент нельзя брать плоский `t(locale,'empty')`: это **шутка sealife** («Тут пока тихо, как
  тюлень после обеда»), и она попадёт на каталог центров, когда фильтр статуса ничего не нашёл.
  DESIGN_BRIEF §4c требует ровно обратного — сухого полезного текста с альтернативным действием.
  Сохранять `Record<SiteId, string>`. (`ListSkeleton` переносить не нужно: `ui/CardGridSkeleton`
  уже обслуживает все настоящие `loading.tsx`.)

* [ ] **MOCK-03** Демо-плашка — только там, где данные действительно выдуманы. *[S]*
  `/report` и `/rescue-quest` зовут `SectionShell` без `demo={false}`, дефолт — `true`. То есть
  настоящая compliance-форма и переписанный BIO-аудитом сценарий безопасности прямо сейчас подписаны
  «Демо-режим: образцы данных». Докблок самого `SectionShell` объясняет, почему это опасно; BIO-аудит
  применил это рассуждение к `/what-to-do` и остановился.
  Порядок важен: `/rescue-quest` расподписывается **сейчас**; `/report` — только **вместе с M2-T04**,
  потому что его форма и правда ничего не отправляет, и до подключения плашка — единственный видимый
  сигнал об этом. Каталог центров плашку **сохраняет** до M2-T02.
  Ассерт отсутствия демо-подписи из `rescue-advice.e2e.spec.ts` расширить на `/rescue-quest`.

* [x] **MOCK-04** Решить, что такое `/rescue-news`. *[S]*
  Раздел с `nav: true` в шапке аварийного сайта, рендерящий **одну выдуманную запись**, и при этом
  `rescue-news` не встречается в этом файле ни разу — у него нет ID, нет модели и нет владельца.
  Коллекции нет; `Content` не подходит: ни у одной коллекции или поля нет дискриминатора `site`
  (проверено grep'ом), а `type: 'news'` уже занят разделом sealife. Сегодня написать sealrescue-новость
  физически негде.
  Три варианта, выбрать осознанно: (а) поле `site` у `Content` — тогда правятся запросы, sitemap и
  catch-all роут; (б) отдельная коллекция; (в) убрать раздел из nav до появления реального контента.
  Самое честное и дешёвое — (в): одна фальшивая запись в навигации аварийного сайта хуже отсутствующего
  раздела. Развилка общая с **CR-02**; `hasDetail: false` в `sections.ts` заодно решает, будут ли у
  rescue-новостей детальные страницы.
  — **сделано 2026-07-28: выбран вариант (в).** Запись раздела убрана из `sections.ts`, роут
  `rescue-news/` удалён, выдуманная `sampleRescueNews` — тоже. Теперь путь отдаёт 404 через
  `getSection()`, а навигация аварийного сайта не содержит пункта, за которым нет ничего.
  Пустой раздел был бы не лучше фальшивого: на emergency-сайте пункт меню — обещание.
  Вернётся вместе с реальной моделью в M2, и тогда же осмысленно заводить дискриминатор `site`
  (см. CR-02). Проверено e2e: `/en/rescue-news?site=sealrescue` → 404; `nav-smoke` подхватывает
  изменение сам, потому что обходит `navSectionsForSite()`, а не хардкод.

* [ ] **MOCK-05** Решить судьбу `/[locale]/styleguide`. *[S]*
  `localization.md` называет её «dev-only витриной компонентов» — код этого не реализует: guard
  проверяет только `isSite`/`isLocale`, значит страница отвечает 200 на **обоих** доменах, в ru и en,
  с захардкоженным русским текстом даже на `/en`. Не залинкована и `noindex`, поэтому экспозиция
  низкая — но решение всё равно нужно принять явно: **QA-26** и **QA-27** назначают styleguide
  обязательной целью, так что «мок вывели» не должно снести её заодно.

### CR — Готовность к наполнению контентом

> Второй результат аудита 2026-07-26: мок-слой и наполнение контентом — **две разные задачи**, и первая
> ничего не говорит о второй. Ниже то, что ломается ровно в тот момент, когда в БД появляется реальный
> текст, а не сеяные черновики. Порядок внутри трека — по цене ошибки, а не по объёму работы.

* [x] **CR-01** ⚠️ **КРИТИЧНО: `fallback: true` отдаёт исходную локаль как переведённую.** *[M]*
  В `payload.config.ts` включён глобальный locale-fallback, и **ни один** публичный запрос от него не
  отказывается (единственный `fallbackLocale: false` во всём репозитории — у глобала section-content).
  Значит статья, написанная только на ru, возвращается **полностью заполненной** под `locale: 'en'`:
  попадает в `/en/articles` с русским заголовком, рендерится на `/en/<slug>` русским телом,
  объявляется `hreflang="en"` и уходит в sitemap как английский URL.
  Это прямое нарушение инварианта №3 (перевод заранее, в хранилище, с hreflang) и оно сработает
  **гарантированно**, потому что рабочий процесс именно такой: пишем ru → переводим позже. Механизм,
  который должен был ловить это, есть, но односторонний: `localeStatus[]` заполняется хуком
  `markTranslationsStale` и **не читается ни одним компонентом**.
  Fix: `fallbackLocale: false` в публичных запросах + не показывать непереведённое (или показывать с
  честной пометкой). Делать **до** первой опубликованной статьи, иначе Google проиндексирует русский
  текст как английский.
  — **сделано 2026-07-26.** Система **fail-closed**: пока перевода нет, документа в этой локали нет.
  `localization.fallback: false` глобально (у смонтированного REST/GraphQL локаль задаёт клиент, и
  опт-аут по месту там недостижим — `glossary` с `read: () => true` уезжал русскими `note` под en).
  Плюс гейт `translatedWhere()` (`src/i18n/translated.ts`) во всех публичных чтениях: списки,
  лента главной, три детальных роута; деталь без перевода → настоящий 404. hreflang и sitemap
  считают доступность **одним** хелпером `localesWithContent()` — разойдись они, hreflang перестал
  бы быть взаимным.
  Формулировка гейта — «в этой локали есть текст», а НЕ «это исходная локаль»: поэтому смена
  направления перевода (**CR-14**) его не трогает.
  Измерения на живой БД зафиксированы `tests/int/locale-fallback.int.spec.ts` и по ходу исправили
  два проектных допущения: (1) одного `fallbackLocale: false` мало — документ всё равно
  возвращается, просто с пустым заголовком, поэтому нужен ещё и `where`; (2) состояния «строка
  локали есть, заголовок пустой» через API не построить (`title` — `required`), так что рабочее
  условие — `exists`, а `not_equals: ''` оставлен страховкой на данные в обход валидации.
  ⚠️ Осталось честно сказанным: выключение флага меняет **дефолт** публичного REST, но не запирает
  дверь — явный `?fallback-locale=ru` Payload обслуживает. Публичные страницы защищены гейтом, а не
  флагом.
  ⚠️ `rescue-centers` **не гейтится никогда**: там локализовано только описание, а прятать
  аварийный контакт из-за непереведённого блурба — нарушение инварианта №7. Записано в
  `src/i18n/translated.ts` рядом с хелпером, чтобы это не «дочинили» в M2-T02.
  ⚠️ Полевой случай, который гейт по заголовку не ловит: массив `species.facts` НЕ локализован,
  локализован только `facts.text`. Вид с переведённым `name` проходит гейт, а отдельные факты могут
  быть пустыми — непереведённые факты отсеиваются на рендере (иначе пустая строка обнуляла бы
  панель «Факт дня» для всей локали).
  Компромисс на будущее, если отставание RU станет проблемой для аудитории из VK/TG: показывать
  непереведённое с честной пометкой + `noindex` + canonical на исходную локаль + исключение из
  sitemap. Сегодня это не нужно — весь контент в черновиках, публичного запуска нет.

* [x] **CR-02** Site-scope канонического роута `/[locale]/[slug]`. *[S]*
  `getDoc()` фильтрует только по slug и `_status: published`, а guard страницы проверяет только
  `isSite`/`isLocale`. Поскольку прокси переписывает оба хоста в сегмент `[site]`, каждая
  опубликованная статья, новость и мем sealife резолвится и на `sealrescue.info`, причём
  `buildAlternates` строит canonical от **запрошенного** хоста — один документ самоканонизируется на
  двух доменах. Списочные роуты защищены (`requireSection` → `getSection`, покрыто e2e), catch-all —
  нет и не покрыт.
  Смягчающее: sitemap уже site-scoped и ни одна страница sealrescue не ссылается на контентный slug,
  то есть crawl-пути сегодня нет — только прямой URL. Развилка общая с **MOCK-04**, поэтому делать
  их вместе.
  — **сделано 2026-07-28, и развилка решена ИНАЧЕ, чем предполагалось.** Дискриминатор `site` не
  заводился. Общий факт у обеих задач другой: **`content` — коллекция sealife**. Все шесть разделов
  sealife (articles, news, memes, quizzes, games, species) читают её, у sealrescue нет ни одного;
  поля `site` нет ни в одной коллекции. Заводить его сейчас значило бы строить механизм под
  редакционный контент sealrescue, которого не существует и чью модель M2 ещё не проектировал.
  Поэтому канонический роут **гейтится по сайту** (`contentSite` в `src/content/contentTypes.ts`,
  туда же сведена и проверка в sitemap, чтобы они не разъехались), а не фильтруется по
  несуществующему полю. Списочные роуты этой дыры не имели — они идут через `getSection()`.
  Когда у sealrescue появится своя лента (M2), правильный ход — не расширять гейт, а завести
  дискриминатор вместе с моделью.

* [x] **CR-03** Publish-pass после BIO-16 + защита сида от затирания реального контента. *[S]*
  Две стороны одной медали. (1) После BIO-16 всё сеяное — черновики, а публичные запросы берут только
  `published`: `/articles`, `/news`, `/memes`, `/species` и лента главной показывают empty state,
  в sitemap одна главная, пока человек не опубликует 9 документов контента и 3 вида.
  (2) `seedM1` делает upsert **по slug** и всегда перезаписывает title/excerpt/topics/body, сбрасывая
  `_status` в draft и провенанс в «черновик писал AI» — по 12 занятым slug'ам (`why-seals-cry`,
  `how-seals-sleep`, `seal-whiskers`, `baltic-center-released-pups`, `new-rookery-spotted`,
  три `meme-*`, `about`, `baltic-ringed-seal`, `grey-seal`, `harbour-seal`). Первая реальная
  статья под таким slug'ом умрёт при следующем прогоне сида. Guard: не трогать документы, чей провенанс
  больше не сидовый, либо требовать явный `--force`.
  Туда же: workflow «Seed» — `workflow_dispatch` с выбором `m1`, без `environment`-гейта и без
  подтверждения, против одного репозиторного `DATABASE_URI`. Три клика в UI GitHub перезаливают то,
  на что этот секрет сейчас указывает.
  — **сделано 2026-07-26.** Сид перестал быть разрушительным: `seedOwnership()`
  (`src/seed/ownership.ts`) обновляет запись, только если она всё ещё выглядит так, как сид её
  оставил. Стоп по любому из трёх независимых признаков: `_status: published` (публикация —
  человеческое действие, инвариант №1), `provenance.humanReviewed`, либо заголовок разошёлся с
  сидовым. Пропуски считаются в `skipped` и печатаются в лог с причиной — молчаливый пропуск так
  же плох, как молчаливая перезапись. Осознанный откат демо-набора к эталону — `seedM1(payload,
  { force: true })`.
  Сравнение идёт с тем, что сид **собирался** записать: сид точно знает своё содержимое, поэтому
  расхождение однозначно означает чужую правку. Отдельный флаг «сеяно» был бы хуже — он
  рассинхронизируется с реальностью.
  ⚠️ Ошибка, найденная прогоном полного набора тестов, а не ревью: первая версия читала
  `existing.title`, но у `species` поле называется `name` — сравнение всегда шло с `undefined`,
  и сид молча пропускал ВСЕ виды как «отредактированные». Тихо и ровно в ту же сторону, что
  исходный баг. API теперь требует передать значение заголовка явным аргументом.
  Workflow «Seed» получил обязательный ввод `confirm`: имя сида набирается руками и сверяется с
  выбранным, иначе запуск падает до `checkout`.
  — **Publish-pass — сознательно РУЧНОЙ.** Автопубликация сеяного контента вернула бы ровно то, что
  запретил BIO-16 (непроверенная биология как публикация) и нарушила бы инвариант №1. После сида
  9 записей контента и 3 вида остаются черновиками, поэтому `/articles`, `/news`, `/memes`,
  `/species` и лента главной честно показывают empty state, а sitemap содержит одну главную.
  Публикует человек в админке. Теперь это безопасно в обе стороны: опубликованное больше не
  сбрасывается обратно в черновик повторным прогоном сида.

* [x] **CR-04** ⚠️ Загруженные медиа **не переживают деплой**. *[M]*
  `Media` объявлена как `upload: true` без `staticDir`, и в зависимостях **нет** ни одного
  `@payloadcms/storage-*`. Payload по умолчанию резолвит `staticDir` от рабочего каталога процесса, а
  systemd-юнит указывает `WorkingDirectory=/opt/sealife/current` — симлинк, который CI перекидывает на
  свежий per-SHA каталог (`rsync --delete`), а релизы старше пятого удаляет. Значит загруженные
  картинки лежат внутри релиза и исчезают на следующем деплое: каждый `coverImage` отдаёт 404.
  Локально это работает идеально, поэтому сломается только после того, как оба сайта наполнены.
  `DEPLOYMENT.md` про медиа и загрузки не говорит ничего.
  Рядом и дешевле: нет `imageSizes` → один оригинал без srcset (`sharp` при этом установлен, то есть
  не хватает конфига, а не зависимости); нет re-encode → файл отдаётся байт-в-байт вместе с EXIF (GPS
  с телефона едет вместе с мемом); нет `focalPoint`/`crop`, а `Cover` режет всё в `16/10` —
  включая **детальную страницу мема**, где картинка и есть содержание; `alt` обязателен и локализован
  под `fallback: true`, поэтому RU-only alt тихо станет «английским» (см. **CR-01**).
  — **сделано 2026-07-26.** Каталог загрузок задаётся `MEDIA_DIR` и обязан быть абсолютным и вне
  `releases/`; логика вынесена в `src/media/storage.ts` (относительный путь отвергается с ошибкой —
  он резолвился бы ровно от подменяемого симлинка). Деплой делает `mkdir -p` shared-каталога и не
  трогает содержимое; при `NODE_ENV=production` без `MEDIA_DIR` приложение предупреждает в лог.
  Раннбук — `DEPLOYMENT.md` §7a, там же записано, что `pg_dump` медиа НЕ покрывает и каталог надо
  бэкапить отдельно (иначе restore даст записи с рабочими путями и отсутствующими файлами).
  — ⚠️ **Главная находка: `sharp` не был передан в `buildConfig`.** Пакет стоял в зависимостях, но
  без явной передачи Payload пишет предупреждение в лог и МОЛЧА игнорирует
  `imageSizes`/`resizeOptions`/`formatOptions`. То есть добавить конфиг размеров было бы
  недостаточно — он бы просто не исполнялся. Проверено загрузкой на живом Payload до и после:
  до — `image/jpeg` без производных, после — `image/webp` и три размера.
  Теперь: оригинал пережимается в webp (≤2400px, без растягивания), производные
  `thumbnail` 400 / `card` 800 / `hero` 1600, `focalPoint`+`crop` включены, `mimeTypes`
  ограничены картинками. Пережатие снимает EXIF — GPS со смартфона больше не уезжает с мемом
  (закреплено тестом: маркер из исходного JPEG отсутствует в сохранённом файле).
  — `Cover` получил режим `fit`: `crop` (16/10, декор-обложка) и `natural` — для детальной
  страницы мема, где картинка И ЕСТЬ содержание и кадрирование резало портретный скриншот.
  `alt` считает `coverAlt()`: после CR-01 alt, заполненный только на исходной локали, приходит
  пустым, и для декора пустой `alt=""` корректен по WCAG 1.1.1, а для смысловой картинки
  подставляется заголовок (он уже прошёл гейт перевода, значит на языке страницы).
  — Не делалось сознательно: внешнее объектное хранилище (S3/R2). Инвариант data-residency держит
  данные в EU/EEA, бокс и так в EU, а облачная зависимость ради «не удаляй каталог при деплое»
  дороже одного env. Не делалось: принудительный alt во всех локалях — это меняет редакторский
  процесс, вопрос к M1-T08.

* [x] **CR-05** У `Content` нет даты публикации. *[S]*
  Поля `publishedAt`/`date` в коллекции нет, поэтому читателю показывается `updatedAt`, и по нему
  же сортируются лента главной, все списки и `lastmod` в sitemap. Через три недели наполнения это
  значит: правка опечатки в мартовской статье поднимает её наверх главной, передатирует перед читателем
  и заново штампует lastmod. Для новостей, которые CLAUDE.md считает journalistisch-redaktionell под
  §18 MStV, «дата = когда я последний раз трогал» — хуже, чем косметика.
  — **сделано 2026-07-27.** Поле `publishedAt` в `Content` (`index: true`, в сайдбаре админки),
  штамп — хук `stampPublishedAt`; правило вынесено в чистую `nextPublishedAt()`
  (`src/content/publishedAt.ts`) и покрыто 9 unit-кейсами.
  Правила: дата ставится при ПЕРВОЙ публикации и больше не двигается (правка опечатки — не новая
  публикация); у черновика даты нет вовсе; выставленное человеком значение не перетирается
  (перенос старого материала, отложенная публикация); снятие с публикации дату не стирает.
  Отображение и сортировки переведены на неё: `ContentDetail`, список новостей, `getContent`,
  `LatestFeed`, порядок в sitemap.
  ⚠️ **`lastmod` в sitemap СОЗНАТЕЛЬНО остался `updatedAt`** — это не недосмотр: lastmod по
  определению «когда последний раз менялось», то есть именно правка. На дату выхода переведены
  порядок и то, что видит читатель.
  ⚠️ **Не локализовано** — намеренно: оригинал и перевод это один материал, вышедший один раз;
  две разные даты выхода одного текста сообщали бы читателю, что материалов два. Дату появления
  перевода при необходимости несёт `localeStatus[].translatedAt` (другое утверждение, см. CR-15).
  ⚠️ Инвариант, закреплённый тестом: **опубликованной записи без даты выхода не существует** —
  включая случай «дату очистили руками». Иначе она уехала бы в НАЧАЛО списков, потому что Postgres
  при сортировке `-publishedAt` ставит NULL первым. Записей, опубликованных до появления поля, в
  БД нет (после BIO-16 весь сеяный контент — черновики), но на всякий случай отображение имеет
  фолбэк `publishedAt ?? updatedAt`.

* [x] **CR-06** Лента главной без фильтра по типу. *[S]*
  `LatestFeed` фильтрует только `_status`, а затем печатает `doc.type` подписью карточки. Публикация
  страницы `about` (один из сеяных slug'ов, `type: 'page'`) поставит карточку «page» наверх главной
  sealife. Тот же класс ошибки поймает любую будущую страницу — контакты, редакционную политику,
  страницу ответственного по §18 MStV.
  — **сделано 2026-07-27.** Фильтр `type: { in: feedTypes }` в `LatestFeed`, источник правды —
  `src/content/contentTypes.ts`.
  Ключевое решение: не массив «разрешённых» типов, а **исчерпывающая таблица** по union
  `Content['type']`. Добавили новый тип в коллекцию → TypeScript не соберётся, пока в таблице не
  появится решение «редакционный или служебный» и подписи на двух языках. Массив молча пропустил бы
  новый тип в ленту (или молча выкинул) — то есть повторил бы этот же класс ошибки.
  — **вторая половина, найденная в тех же трёх строках:** кикер карточки печатал `doc.type` СЫРЫМ,
  и читатель видел `article` вместо «Статья» — причём только в ленте, на детальной странице подпись
  была локализована. Подписи из `ContentDetail` (жили там приватной копией) переехали в общий
  модуль, лента их использует.
  Проверено на живой странице: в ленте одна карточка с кикером «Article», служебная страница
  отсутствует. Плюс int-тест намеренно фиксирует и сам баг — без фильтра страница в выборку
  попадает, то есть тест доказывает, что работает именно фильтр.

* [ ] **CR-07** Пагинации нет нигде: `pagination: false` на всех списках. *[M]*
  Все три query-хелпера и оба запроса sitemap читают таблицу целиком; в UI нет ни `page`/`limit`, ни
  «показать ещё» (единственный лимит — 20 в ленте главной). Невидимо на 9 черновиках, первая жалоба —
  на 50–100 документах, то есть ровно на горизонте M1-T07 (20 evergreen-статей) плюс новости плюс мемы.
  Хуже всего галерея мемов: обложка каждого приезжает с `depth: 1` и рендерится полноразмерным `<img>`.

* [x] **CR-08** Нет предпросмотра черновика. *[S]*
  Ни `admin.preview`, ни `livePreview`, ни `draftMode()`, ни `draft: true` хоть в одном запросе.
  Вместе с BIO-16 (всё сеется черновиком) и published-only публичными запросами редакторский цикл
  выглядит так: написал в админке → **опубликовал** → посмотрел на живом сайте → починил. Для наполнения
  двух сайтов локализованным richText с обложками и провенансом это самая раздражающая вещь из всего
  списка, и она повторяется на каждом документе.
  — **сделано 2026-07-28.** Кнопка «Preview» в админке у `content` и `species` ведёт на
  `/api/preview`, тот включает Next-овский draft-режим и редиректит на публичную страницу;
  детальные роуты в этом режиме читают с `draft: true` и без фильтра по `_status`.
  ⚠️ **Вся защита сосредоточена в одном обработчике**, и это главное в задаче: draft-режим — это
  cookie, то есть включивший его браузер видит черновики. Поэтому `/api/preview` требует сессию
  сотрудника (роли как в `readPublishedOrStaff`, кроме служебного `agent`) и принимает **только
  относительный путь** — иначе эндпоинт становится открытым редиректом с нашего домена, да ещё с
  включённым draft-режимом. Обе проверки закреплены e2e, включая разделение причин отказа:
  внешний путь → 400, валидный путь без сессии → 401. Если бы всё подряд отвергалось как «плохой
  путь», дыра в авторизации осталась бы незамеченной.
  ⚠️ **Гейт перевода (CR-01) в предпросмотре НЕ отключается**: показывать русскую страницу
  английским текстом неверно и редактору тоже. Снимается только фильтр `_status`.
  ⚠️ **Область — только детальные страницы.** Списки, лента главной и sitemap остаются
  published-only: предпросматривают конкретный документ, а расширение draft-режима на списки
  увеличило бы поверхность утечки без пользы.
  — Плашка режима с кнопкой выхода обязательна, а не украшение: draft-режим внешне неотличим от
  обычного сайта, и без неё редактор остаётся в нём, принимая черновики за опубликованное.
  — Не делалось: `livePreview` (правка бок о бок с рендером) — это отдельная задача про UX
  админки, а не про «увидеть черновик до публикации».

* [ ] **CR-09** Sitemap и robots после наполнения. *[S]* → уточняет **M1-T15**
  Sitemap содержит главную плюс slug'и контента и видов — и больше ничего: ни одной секционной
  страницы (`/articles`, `/news`, `/memes`, `/species`, `/games`, `/quizzes` отсутствуют), ни
  legal-страниц, а для sealrescue — буквально только главная, то есть `/what-to-do` (смысл
  существования второго сайта) не подаётся поисковикам вообще. `robots.txt` нет нигде, так что на
  sitemap ничто не указывает. Оба факта закреплены e2e, значит это контракт, а не недосмотр, — и менять
  его надо явным решением.

* [ ] **CR-10** Ни OG, ни JSON-LD, ни favicon. *[M]* → сводит **M1-T11** + **M1-T16**
  Grep по `src/` на `openGraph`, `twitter`, `ld+json`, `schema.org`, `metadataBase` — пусто; ни
  `favicon`/`icon`/`apple-icon`/`opengraph-image`/`manifest` ни для одного из сайтов (единственный
  favicon в репозитории принадлежит бандлу игры). `generateMetadata` ставит title, description и
  alternates — и всё. Аудитория проекта приходит из сообществ VK/TG, то есть **каждая** ссылка, которую
  владелец туда положит, отрисуется голым заголовком без картинки и с дефолтным глобусом во вкладке.

* [ ] **CR-11** Overrides разделов не доходят до главной sealrescue. *[S]*
  M1-T27 позволяет редактору переписать title/intro/обложку каждого раздела через глобал
  `section-content`, и это подключено к страницам разделов и к хабу главной sealife. Но
  `SealrescueHome` рендерит `s.label`/`s.intro` прямо из `sections.ts`. Редактор перепишет интро
  «Центры» — увидит правку на `/ru/rescue-centers` и не увидит на главной, без единого намёка почему.
  (Тот же компонент рисует все пять разделов sealrescue, включая `rescue-quest`, который помечен
  `nav: false` сознательно.)

* [ ] **CR-12** Нет global-error boundary. *[S]*
  `[site]/[locale]/layout.tsx` — корневой layout группы `(frontend)`: он рендерит `<html>`/`<body>`,
  и выше него layout'а нет. Next-овский `error.tsx` не ловит ошибки layout'а **своего** сегмента, а
  `global-error.tsx` в проекте отсутствует. Значит любое падение в layout, `SiteHeader`,
  `SiteFooter` или компонентах consent даст дефолтную неоформленную страницу Next вместо
  спроектированного `ErrorBlock`.

* [ ] **CR-13** Каждый запрос выполняется дважды. *[S]*
  React `cache()` в проекте не используется нигде, а Next дедуплицирует только `fetch`, но не local
  API Payload. Поэтому `[slug]/page.tsx` зовёт `getDoc` в `generateMetadata` и ещё раз в теле, а
  каждый секционный роут дважды резолвит section-overrides: 2 `findGlobal` + 1 `find` на просмотр
  раздела и 2 полных `find` на просмотр статьи. Всё это SSR per-request, ISR нет. Чинится дёшево
  сейчас и неприятно — потом, поверх ISR.

* [x] **CR-14** Развернуть направление перевода: исходная локаль **ru → en**. *[M]*
  Решение владельца 2026-07-26. Причина содержательная, а не техническая: подавляющее большинство
  источников и исследований по морским млекопитающим — англоязычные, и агенты-факт-чекеры
  (инвариант №8) проверяют факты по ним же. Писать исходник на языке источников — значит не терять
  точность на двойном переводе. Разворачивается **стоящий инвариант** (`CLAUDE.md` №3), поэтому
  решение и его причина записаны здесь, а не только в коммите.
  — **сделано 2026-07-26.** Правка оказалась конфигурационной ровно потому, что **CR-01** приехал
  первым и сформулировал гейт как «в этой локали есть текст», а не «это исходная локаль»:
  `src/app/(frontend)/` этот PR не трогает вовсе.
  Что поменялось: `defaultLocale: 'en'` (`targetLocales` выводится сам, хук
  `markTranslationsStale` читает оба оттуда и правок не потребовал); последний рубеж `t()` теперь
  словарь исходной локали, а не `dict.ru` — иначе пропущенный ключ показал бы английскому читателю
  русскую строку; сид параметризован по `defaultLocale`/`targetLocales`; тесты направления
  импортируют константы вместо литералов, литералы остались ровно в
  `tests/unit/i18n.unit.spec.ts`.
  ⚠️ **Провенанс привязан к `authoredIn` записи, а не к направлению.** У каждой записи сида
  появилось поле `authoredIn` (весь нынешний корпус — `'ru'`), из него выводятся флаги. Слепой
  своп констант пометил бы русские оригиналы «машинным переводом», а машинные переводы —
  «черновиком AI»: ложное заявление о происхождении по AI Act Art. 50, которое читатель видит
  бейджем на каждой странице (инвариант №5).
  ⚠️ `fallbackLocale` остался `'en'` и теперь СОВПАДАЕТ с `defaultLocale` — по несвязанной
  причине: это политика выбора языка для посетителя, а не язык исходника. В
  `tests/unit/i18n.unit.spec.ts` они проверяются отдельными тестами именно для того, чтобы кто-то
  не схлопнул их в одну константу.
  ⚠️ Порядок массива `locales` НЕ меняли: из него выводится порядок union в `payload-types.ts`.
  Долг, зафиксированный отдельно: существующий корпус написан на ru, а исходной локалью объявлен
  en — то есть 12 сеяных документов и всё, что редактор напишет до перевода, живут «переводом
  вперёд исходника». Сегодня это админ-видимая мелочь (весь контент в черновиках), но при массовом
  наполнении вопрос вернётся. Строки `localeStatus` со старым направлением остаются в БД до
  следующей записи исходной локали; их никто не читает (см. долг ниже).

* [ ] **CR-15** `localeStatus` не работает: писатель есть, читателей нет, хэш не сохраняется. *[S]*
  Всплыло при разборе CR-01/CR-14 как кандидат на гейт перевода — и оказалось непригодным.
  `markTranslationsStale` считает `sha256` исходника, но записывает `prev?.sourceHash ?? null`, то
  есть посчитанный хэш никогда не сохраняется; поле вдобавок `admin: { readOnly: true }`. Поэтому в
  проде `status` навсегда залипает в `stale`, а int-тест доходит до `current` только потому, что
  подаёт хэш сам. Читателей в `src/app/` — ноль.
  Это ОТДЕЛЬНЫЙ от CR-01 вопрос и не должен прятать страницы: «перевод существует» и «перевод
  устарел» — разные утверждения, и скрывать можно только по первому.

### DESIGN — Дизайн-система

* [~] **DESIGN-01** Держать токены едиными; уникальность — в signature-элементах, не в хаосе. *[S]*
  — **аудит 2026-07-26:** слоение токенов реальное (primitive → semantic → два режима через
  `data-site`), styleguide есть. Не хватает **принуждения**: правило «в компонентах только semantic»
  ничем не проверяется (в eslint-конфиге ограничен только `waitForTimeout`) — это QA-28.
* [~] **DESIGN-02** Не перекрашивать админку Payload; dashboard агентов — плотный, без декора. *[S]*
  — **аудит 2026-07-26:** половина «не перекрашивать админку» ПРОВЕРЕНА: `src/app/(payload)/custom.scss`
  — файл нулевого размера. Половина про dashboard агентов непроверяема: дашборда ещё нет (M2-T12).
* [~] **DESIGN-03** Производительность: SVG-дудлы, self-host шрифты, бюджет Core Web Vitals. *[S]* → SEO
  — **аудит 2026-07-26:** практики применены: self-host шрифты через `next/font`, Phaser грузится
  ленивым `import()` только по «Старт», векторный арт вместо растра. Не хватает: бюджет CWV нигде не
  измеряется и не ассертится.

### QA — Качество (расширенный план тестирования)

> **Правило проекта: любая фича без теста — баг** (закреплено в `CLAUDE.md` → Dev best practices
> и `docs/local-development.md` § Тесты). Сайт активно развивается, и изменения регулярно ломают
> соседний функционал; единственная защита — чтобы каждый контракт был закреплён зелёным тестом,
> падающим при поломке ИЛИ при устаревании самого контракта.
>
> **Модель — test trophy, слои снизу вверх:**
> 1. **Unit** (Vitest, без БД/DOM, < 5 c): чистая логика — i18n, alias/PRNG, season-математика, хуки.
> 2. **Integration** (Vitest + Payload local API + тестовая БД): access-матрица, хуки на записи,
>    endpoints (лидерборд), сиды.
> 3. **E2E** (Playwright против `next build && next start`): роутинг/страницы/UI-контракты/игра.
> 4. **Специальные слои:** a11y (axe), визуальная регрессия (скриншоты styleguide), перф (Lighthouse
>    CI + CWV-бюджеты), детерминированная симуляция для игр (seeded golden runs).
>
> **Конвенции:** тесты детерминированы (фиксированный seed, замороженное время где надо); никаких
> sleep-ожиданий — только web-first assertions; сетевые зависимости в e2e мокируются
> (`tests/e2e/helpers/mock-leaderboard.ts` — образец); фикстуры не зависят от dev-данных.
> Порядок внедрения: **A (гейт) → B (инварианты бэкенда) → C/E (пользовательские контракты) → D/F**.

#### QA-A — Фундамент: CI-гейт и слои тестов

* [x] **QA-08** CI-workflow `test.yml`: `lint` + `tsc --noEmit` + `test:int` на каждый PR и push в
  `main`; тестовая БД — ephemeral Postgres (service container, образ **postgis/postgis** — поле
  `point` у RescueCenters требует PostGIS), схему создаёт push-режим Payload. Сделано 2026-07-02.
  Required check `test` на `main` ВКЛЮЧЁН (branch protection; репо стало public 2026-07-02):
  мерж в `main` невозможен на красном `test`; strict=false (PR не обязан быть up-to-date),
  enforce_admins=false. Шаг `test:unit` добавится при разнесении слоёв (**QA-10**); drift-чек
  `generate:types` остался в **M0-T07**. Закрывает **QA-06**. *[M]*
  — **аудит 2026-07-26:** устаревшая приписка: «Шаг `test:unit` добавится при разнесении слоёв (QA-10)»
  — QA-10 сделан, `test:coverage` уже гоняет оба проекта (unit + int).
* [x] **QA-09** Playwright в CI: job `e2e` в `test.yml` — схема через `scripts/push-dev-schema.mts`
  → `next build` → Playwright сам поднимает `next start` (webServer, ветка CI) → полный e2e-прогон
  на chromium headless shell (channel-пин снят); трейсы/скриншоты — артефакты при фейле; retries=2
  и `reuseExistingServer:false` только в CI; кэш браузеров по версии Playwright. Включает обратно
  game-leaderboard-scroll (**QA-07**). Заодно починен локальный `test:e2e`: webServer теперь
  `npm run dev` (был `pnpm dev` — не поднимался без pnpm). Сделано 2026-07-02. *[M]*
* [x] **QA-10** Разнести Vitest на `test:unit` (чистая логика, без БД, секунды) и `test:int`
  (Payload+БД); coverage (V8) с порогом на `src/` — старт 40%, повышать по мере покрытия. *[M]* —
  сделано 2026-07-02: vitest `projects` (unit: node-env `tests/unit/`, int: jsdom `tests/int/`);
  скрипты `test:unit`/`test:int`/`test:coverage`; в CI job `test` гоняет `test:coverage`.
  Стартовый unit-слой — 6 спеков (~50 тестов): access-матрица ролей (срез QA-13), resolveSiteId,
  инварианты локалей + t() + buildAlternates, alias-рендер лидерборда (срез QA-16), factOfDay,
  sections/legal/formatDate. Пороги (ratchet, только вверх): lines/statements/functions 40,
  branches 18 (ветвление в endpoint-ветках leaderboard.ts — поднять до 40 с **QA-15**).
  Coverage include: `src/**` без `src/app` (e2e-территория), payload-types, seed, mock.
* [x] **QA-11** Изоляция тестовой БД: отдельная схема/БД на прогон + сид фикстур в `beforeAll`;
  int-тесты не читают и не портят dev-данные. *[M]* — сделано 2026-07-02: в CI — ephemeral
  Postgres на прогон (с QA-08); локально int-тесты идут в `DATABASE_URI_TEST` (отдельная
  БД/Neon-ветка, `.env.example`), подмена в `vitest.setup.ts` с громким предупреждением при
  фолбэке на dev-БД. E2e сознательно вне механизма (сид и сервер должны смотреть в одну БД).
  Сид фикстур: паттерн `tests/helpers/seedUser.ts`; полные фикстуры придут с QA-13/15/18.
* [x] **QA-12** Политика флаки: quarantine-тег, запрет `waitForTimeout` в пользу assertions,
  максимум 2 ретрая в CI, флаки-тест чинится или удаляется в течение недели. Задокументировать
  в `local-development.md`. *[S]* — сделано 2026-07-02: `waitForTimeout` запрещён ESLint-правилом
  (`no-restricted-syntax` для `tests/**`); тег `@quarantine` исключается в CI (`grepInvert` в
  playwright.config.ts), локально бежит; retries=2 CI-only (было с QA-09); политика —
  `local-development.md` § Тесты → Конвенции.

#### QA-B — Бэкенд: access control, хуки, endpoints, сиды

* [x] **QA-13** Access-матрица ВСЕХ коллекций (14 шт.) параметризованным int-тестом: роли
  (anon/viewer/translator/editor/admin/agent) × операции (create/read draft+published/update/delete/
  publish) × ожидание из `data-model.md`. Инварианты №1–2 CLAUDE.md (agent никогда publish/delete)
  — отдельные явные ассерты. Расширяет **QA-01**, закрывает проверочную часть **SEC-06**. *[L]* —
  сделано 2026-07-02: `tests/int/access-matrix.int.spec.ts`, 110 тестов (матрица C/U/D по 14
  коллекциям, boolean-read группы, read-фильтр черновиков, least-privilege `users`,
  `forceAgentDrafts` create+update, контроль «editor публикует»). Заодно закрыт **SEC-07**
  (явный access у `media` — тест иначе падал) и поднят coverage-ratchet
  (lines 45 / stmts 46 / funcs 55 / branches 25). Int-файлы сериализованы
  (`fileParallelism: false`) — параллельный boot Payload гонял drizzle push наперегонки.
* [x] **QA-14** Хуки контента: `forceAgentDrafts` (запись от роли agent всегда `_status: draft`,
  даже при явном `published` в data) и `markTranslationsStale` (смена RU помечает перевод(ы) —
  после снятия DE это только `en` — stale;
  partial-update не теряет `localeStatus`; hash стабилен при no-op записи). Unit + int. *[M]* —
  сделано 2026-07-02: `tests/unit/content-hooks.unit.spec.ts` (8) + `tests/int/content-hooks.int.spec.ts`
  (5, live Payload: create→stale, фиксация перевода hash'ем, no-op, partial, EN-запись не трогает).
  Тест выявил и починил баг: partial-update без `title`/`body` (напр. только topics) считал hash
  от пустых строк и ложно помечал переводы stale — теперь значения подставляются из `originalDoc`.
* [x] **QA-15** Контракт лидерборда (int, все ветки `src/endpoints/leaderboard.ts`):
  happy-path start→submit→read; Zod-отказы (400); `invalid_token`(401), `token_age`(422),
  `duration_mismatch`(422), `implausible_duration/score`(422), `token_used`(409, одноразовость
  nonce), `rate_limited`(429), `unknown_game`(404); upsert-max одного игрока; смена name-списков →
  освежение alias; suffix-дедуп при коллизии base двух игроков; пагинация GET (`page`/`limit`/
  `hasMore`); прунинг прошлых сезонов. *[L]* — сделано 2026-07-02:
  `tests/int/leaderboard.int.spec.ts` (17 тестов, все перечисленные ветки + lazy prune через
  expect.poll + GET-дефолты на кривых параметрах). Хендлеры вызываются напрямую с Web Request;
  возраст токена — фейк ТОЛЬКО Date (`vi.useFakeTimers({ toFake: ['Date'] })`), без sleep;
  rate-limit изолирован уникальным `x-forwarded-for` на запрос. Coverage-ratchet поднят до
  lines/stmts/funcs 80, branches 65 (актуалы 88/87/88/74 — план «branches 40» перевыполнен).
* [x] **QA-16** Alias-контракт server↔client (страховка «KEEP IN SYNC» в `leaderboard.ts` ↔
  `core/alias.js`): golden-вектор `(seed, game) → canonical EN alias`, прогоняемый обоими
  движками; расхождение списков/PRNG/порядка бросков валит тест. *[M]* — сделано 2026-07-02:
  `tests/unit/alias-contract.unit.spec.ts` — 6 golden-векторов (EN + RU/DE с правилами рода:
  ж.р. «Брызгучая Русалочка», DE-род от суффикса «Spritziger…Klops»/«Glitschiges…Brötchen»)
  + sweep 300 (seed,game)-пар по всему uint32-диапазону: parts и EN-рендер движков идентичны.
  Клиентский JS импортируется в vitest напрямую (node-safe). **NB:** DE-векторы здесь про
  собственный словарь игры (`core/alias.js`), который не следует за локалями сайта и пережил
  снятие немецкого как языка сайта (см. SH-09).
* [x] **QA-17** Unit season-математики: `currentSeason`/`seasonEnd` на граничных датах (смена года,
  ISO-неделя 52/53, вс→пн UTC); детерминизм `mulberry32`/`hashStr`. *[S]* — сделано 2026-07-02:
  `tests/unit/season.unit.spec.ts` — вс→пн, декабрь→W01 следующего ISO-года (2025-12-29 →
  2026-W01), 53-недельный 2026 (W53 до 2027-01-03), ведущий ноль недели, инварианты на 60 днях
  (конец всегда будущий понедельник; сезон стабилен до конца и меняется ровно в него);
  `currentSeason`/`seasonEnd` экспортированы. Детерминизм PRNG — в alias-спеках (QA-10/16).
* [x] **QA-18** Сиды: `seed:baseline`/`seed:glossary`/`seed:m1` идемпотентны (повторный прогон не
  дублирует записи); после сида выполняются инварианты (обе контент-локали заполнены, slug каноничны). *[M]* —
  сделано 2026-07-02: логика сидов вынесена в `src/seed/lib.ts` (экспортируемые функции; общий
  games-цикл baseline/m1 дедуплицирован), entry-скрипты — тонкие `payload run`-обёртки (пути/скрипты
  не менялись). `tests/int/seeds.int.spec.ts` (7, state-agnostic — работает и на свежей CI-БД, и на
  посеянной dev): второй прогон created=0 и counts стабильны; games/content — title в обеих локалях,
  published, slug канонический; species — факты не размножаются, id-matching en↔ru; glossary —
  ровно одна строка на source, translation ru=термин/en=перевод. Сиды больше не пишут `de`
  (снятие DE 2026-07-26). **Блок QA-B закрыт целиком.**

#### QA-C — Фронтенд: роутинг, i18n, страницы, consent

* [x] **QA-19** Unit `proxy.ts`: `pickLocale` (cookie > Accept-Language с q-весами > fallback `en`;
  неподдерживаемый язык → `en`, в т.ч. `de`-браузер после снятия немецкого как контент-локали;
  путь `/de/privacy` не редиректится — `routeLocales`) и `resolveSiteId` (host/override); `x-site`/`x-locale` ставятся
  при rewrite. *[S]* — сделано 2026-07-02: `tests/unit/proxy.unit.spec.ts` (13 тестов на реальном
  `NextRequest`): cookie>header>fallback, q-веса против порядка, региональные теги, первый
  ПОДДЕРЖИВАЕМЫЙ из ранжированных; redirect-контракт (307 + Vary + сохранение пути/query);
  rewrite-контракт через x-middleware-заголовки (rewrite-цель /[site]/…, x-site/x-locale,
  host→sealrescue, ?site=override, «прокси не ставит NEXT_LOCALE»). `pickLocale` экспортирован;
  `resolveSiteId` был закрыт в QA-10.
* [x] **QA-20** hreflang/canonical/sitemap: unit на `buildAlternates` (контент, ru/en) и
  `buildLegalAlternates` (legal, ru/en/de); e2e-ассерты альтернатов на обеих контент-локалях
  (перекрёстные + x-default); контракт `sitemap.xml` (только published; пока только
  sealife — ассерт текущего поведения, обновить при M2). *[M]* — сделано 2026-07-02:
  `tests/e2e/seo.e2e.spec.ts` (7): canonical+alternates на главных, странице раздела и
  контентной странице (прод-домены, x-default→en, canonical slug общий); sitemap — главные,
  published-контент с hreflang, черновик исключён, sealrescue-вариант только с главной и своим
  доменом. Фикстуры (published+draft) сидятся Payload'ом из спека. `buildAlternates` — с QA-10.
  Обновлено под две локали 2026-07-26 (контент `/de` больше не альтернат; legal — по-прежнему
  ru/en/de).
* [~] **QA-21** Контент-страницы по seeded-данным (e2e): деталь article/news/meme/species рендерится
  на обеих локалях (`/ru`, `/en`) и отдаёт 404 под `/de`; AiBadge/provenance видимы; TopicFilter
  фильтрует и держит URL-параметр; FactOfDay детерминирован по дате (заморозить clock). *[M]*
  — **аудит 2026-07-26:** частично уже зелено в других спеках: детерминизм «Факта дня» (unit) и 404
  контента на `/de` (e2e). Осталось: e2e-рендер детали article/news/meme/species из seeded-данных,
  ассерт `AiBadge` и тест URL-параметра TopicFilter.
* [x] **QA-22** Legal-shell (e2e): 4 legal-роута × 3 route-локали (`ru`/`en` + legal-only `de`)
  отвечают 200; DE рендерит «Impressum»/«Datenschutz»; legal-ссылки присутствуют в футере каждой
  публичной страницы. *[S]* — сделано
  2026-07-02: `tests/e2e/legal.e2e.spec.ts` (9): 12 роутов по 200; DE-заголовки
  Impressum/Datenschutzerklärung/Nutzungsbedingungen; draft-плашка placeholder-контента на всех
  route-локалях; футер-ссылки (slug/подписи из `src/site/legal.ts`) на 6 типах страниц — главные обоих
  сайтов, раздел, mock-деталь, legal и 404. После снятия DE как языка сайта (2026-07-26) спека
  остаётся в силе — но `/de`-страницы теперь единственные под этим префиксом, и их chrome
  (навигация/футер) английский, тогда как сами документы немецкие.
* [x] **QA-23** Consent/аналитика (e2e, КРИТИЧНО — §25 TDDDG): до opt-in скрипт Plausible НЕ
  загружается (network-ассерт); Accept подгружает; Reject/отзыв отключает; выбор хранится только
  в consent-cookie (не в localStorage); кнопки Accept/Reject равнозначны; Cookie-Settings
  доступна из футера и реально переключает состояние. *[M]* — сделано 2026-07-02:
  `tests/e2e/consent.e2e.spec.ts` (5 тестов): network-перехват фиктивного
  `NEXT_PUBLIC_PLAUSIBLE_SRC=https://plausible.test/...` (задаётся в CI-job `e2e`; без переменной
  спек пропускается — CI остаётся точкой принуждения); равнозначность кнопок ассертится
  computed-стилями (font/bg/height); отзыв через футер → Cookie-Settings → reload выгружает
  скрипт; повторное включение без перезагрузки; localStorage пуст после выбора.
* [x] **QA-24** LanguageSwitcher (e2e): открытие/закрытие (клик вне, Esc, уход фокуса);
  выбор ставит `NEXT_LOCALE` ТОЛЬКО по явному клику; путь сохраняется при переключении локали;
  aria-атрибуты (haspopup/expanded/current). *[S]* — сделано 2026-07-02:
  `tests/e2e/language-switcher.e2e.spec.ts` (5): aria-контракт (haspopup/expanded, текстовые
  пункты по числу контент-локалей — после 2026-07-26 их два, Русский/English,
  aria-current+hreflang у активной); закрытие Esc/кликом вне/Tab'ом наружу; путь
  сохраняется (/en/articles→/ru/articles) и `<html lang>` меняется; cookie ТОЛЬКО по клику
  (навигация/редиректы не создают) + после выбора `/` уважает cookie; свитчер и в футере
  (открывается вверх).
* [~] **QA-25** Report/notice-форма (e2e): поля email НЕТ (ассерт отсутствия — инвариант №4);
  сабмит уходит в премодерацию (submission со статусом pending); empty/error/success-состояния.
  Дополнить rate-limit-тестом после **SEC-05**. *[M]* — сделано 2026-07-02 в объёме существующего:
  `tests/e2e/report-form.e2e.spec.ts` (3: нет email-полей + плашка + контакт оператора в
  legal-notice; required блокирует пустую отправку; success role=status) и
  `tests/int/user-submissions.int.spec.ts` (5: анонимный create без аккаунта → pending;
  **самоодобрение закрыто** — status в анонимном create отбрасывается field-access'ом (баг найден
  тестом: поле было без access, аноним мог прислать `status: approved`); anon update/read
  Forbidden; editor одобряет). Остаток при M2-T04 (форма сейчас ДЕМО, ничего не шлёт): e2e-ассерт
  «сабмит формы создаёт pending-строку»; rate-limit — после **SEC-05**.

#### QA-D — Дизайн-система, доступность, визуальная регрессия

* [ ] **QA-26** A11y-автоматизация: `@axe-core/playwright` на ключевых страницах (обе главные,
  список, деталь, legal, 404, styleguide) в обоих `data-site`-режимах; serious/critical
  нарушения = fail. Ручной чек-лист WCAG 2.2 AA поверх — остаётся в **EU-04**. *[M]*
* [ ] **QA-27** Визуальная регрессия: `toHaveScreenshot` по styleguide (все компоненты в обоих
  режимах) + главные обоих сайтов + 404; маскировать динамический контент; baseline в git;
  прогон в CI на фиксированном viewport/шрифтах. *[M]*
* [ ] **QA-28** Токен-инварианты как статический тест: в компонентах только semantic-токены
  (запрет raw `--baltic` и др. primitive), `--buoy` не используется под белым текстом,
  Baloo 2 отсутствует в кодовой базе. Скрипт/ESLint-правило в CI. *[S]*
* [ ] **QA-29** Состояния UI (e2e): loading/empty/error/populated через `StateSwitcher` на
  mock-разделах; скелеты списков реально показываются (CPU/network throttle) и не вызывают
  layout shift (CLS-ассерт). *[M]*
  — **аудит мока 2026-07-26:** формулировка привязана к `StateSwitcher` (`?state=`), который живёт
  только на двух мок-разделах (quizzes, rescue-news) и уезжает вместе с моком — задачу надо
  переписать под реальные payload-backed состояния, иначе она станет неисполнимой. Сейчас у состояний
  UI **нет ни одного** e2e: `state=` в `tests/` не встречается.

#### QA-E — Игры (Seal The Hunter сейчас; шаблон для всех будущих)

* [x] **QA-30** Sim-детерминизм (golden run): фиксированный seed → идентичный лог состояний и
  финальный счёт `core/sim.js`; любые изменения физики/спавна осознанно обновляют golden-файл.
  Прогон как `test:unit` (sim DOM-free — уже готов к этому). *[M]* — сделано 2026-07-03:
  `tests/unit/sim-golden.unit.spec.ts` + `tests/golden/seal-hunt-sim.golden.json` (60 посекундных
  снапшотов + финальный счёт, квантование 1e-3 от дрейфа transcendental-функций между V8);
  обновление — осознанно через `UPDATE_GOLDEN=1`. Тест НАШЁЛ утечку детерминизма: `edgeBag`
  (спавн-мешок рёбер) тянулся между раундами — добавлен `resetSpawnState()` (prey.js), харнесс
  сбрасывает его на каждый раунд; SW-кэш бампнут (v14).
* [x] **QA-31** Fairness-регрессия: сокращённый прогон `tools/fairness-sim.mjs` в CI с ассертом
  порогов (desktop/mobile catch-rate в допуске 2:1 clamp из `balance.js`); полный прогон — вручную
  при изменении баланса. *[M]* — сделано 2026-07-03: логика харнесса вынесена в
  `tools/fairness-lib.mjs` (CLI и CI гоняют ОДИН код); `tests/unit/fairness.unit.spec.ts` —
  6 представительных профилей × 12 seeded-раундов (~6 с): разброс ≤15%, каждый профиль ±10% от
  среднего, кламп 2:1 (`VIEW_CFG.maxAspect`) закреплён ассертом на всех 16 профилях. Бонус фикса
  edgeBag: честный базлайн разброса упал 9.3% → **5.5%** (carry-over добавлял ложный шум в
  common-random-numbers сравнение).
  — **аудит 2026-07-26:** описание УСТАРЕЛО: в строке всё ещё «в допуске 2:1 clamp», но тест ассертит
  фиксированное поле 16:9 / 9:16 — кламп 2:1 убран в SH-12. Переформулировать.
* [~] **QA-32** Game e2e (полный цикл, mock-leaderboard): старт → ввод (клавиатура+тач-эмуляция) →
  конец раунда → interstitial (3 c) → board с автоскроллом к строке игрока (поглощает **QA-07**);
  standalone vs embedded (есть база); `visibilitychange`-пауза; resize/fullscreen с сохранением
  2:1 clamp. *[L]*
  — **аудит 2026-07-26:** уже зелено: standalone-vs-embedded и переходный экран + автоскролл к строке
  игрока (с mock-хелпером). Осталось: прогон ввода (клавиатура/тач), пауза по `visibilitychange`,
  проверка ресайза/фуллскрина. Из формулировки убрать «сохранением 2:1 clamp» — SH-12 заменил кламп
  фиксированным полем.
* [ ] **QA-33** Service worker: смена версии `CACHE` в `sw.js` подхватывает новую версию по
  network-first (e2e с двумя «релизами»); `manifest.webmanifest` валиден. *[S]*
* [ ] **QA-34** Перф-бюджет игры: FPS-замер в e2e (метки rAF, порог на CI-профиле), суммарный
  размер статики игры < бюджета (ассерт в CI); без регрессий при добавлении ассетов. *[M]*
* [x] **QA-35** Шаблон QA для новых игр (Seal Run и далее): DOM-free sim-ядро + seeded harness +
  golden run + контракт лидерборда + e2e-смоук + fairness-пороги обязательны **с первого PR**
  (зеркалит SR-аудит: изоморфный course-модуль, token-pinned courseSeed). Задокументировать как
  чек-лист в `docs/local-development.md` § Тесты. *[S]*
  — **аудит 2026-07-26:** единственный дельверабл — чек-лист для новых игр — уже написан в
  `docs/local-development.md` § Тесты (DOM-free sim-ядро, seeded golden-run, контрактные тесты
  лидерборда, e2e-смоук, fairness-пороги). Опционально дописать два пункта из аудита SR: изоморфный
  модуль трассы и `courseSeed`, пиннящийся в токен.

#### QA-F — Перф, SEO, ссылки, прод-смоук

* [ ] **QA-36** Lighthouse CI с бюджетами (расширяет **QA-03**): perf ≥ 90, a11y ≥ 95, LCP/CLS/INP
  бюджеты на главных + деталях обоих сайтов; отчёт как артефакт PR. *[M]*
* [ ] **QA-37** Link checker (реализует **QA-02**): обход по sitemap + навигации, включая
  hreflang-альтернаты и футер-ссылки; битые внутренние ссылки = fail; внешние — warning. *[S]*
* [~] **QA-38** Пост-деплой smoke в `deploy.yml`: healthcheck, критические роуты (главные ×2 локали,
  legal ×3 route-локали (вкл. `/de`), игра, `/api/leaderboard` GET) отвечают 200, контент под `/de`
  отдаёт 404, security-заголовки на месте; фейл = алерт. *[S]*
  — **аудит 2026-07-26:** один health-check в `deploy.yml` уже есть. Не хватает матрицы роутов (обе
  главные × 2 локали, legal × 3 route-локали, игра), ассерта «контент на `/de` → 404» и
  security-заголовков. Цель проверки теперь задаётся входом `health_url` — нацеливать на QA-stage.

#### Задел (сделано / унаследовано)

* [x] **QA-01** Тесты access control (agent не может publish/delete). *[M]* — сделано в **QA-13**
  (2026-07-02, `tests/int/access-matrix.int.spec.ts`)
* [ ] **QA-02** E2E проверка ссылок (Playwright link checker). *[S]* → реализуется в **QA-37**
* [ ] **QA-03** Lighthouse в CI. *[S]* → реализуется в **QA-36**
* [x] **QA-04** E2E-тест мультилокальных route guards (контент рендерится на `/ru` и `/en`; `/de` —
  только legal-роуты; неизвестная локаль/slug и любой контент под `/de` → 404). *[S]* — сделано в
  `tests/e2e/frontend.e2e.spec.ts` (PR #39 + real-404 fix
  2026-07-02): брендинг/`<html lang>`/свитчер по локалям, redirect-политика, настоящие HTTP 404
  для несуществующих slug и чужих разделов, локализованная `not-found.tsx`. Гард `/de`-контента
  ужесточён снятием DE (2026-07-26): контент-страницы валидируют `isLocale`, layout и 4 legal-роута
  — `isRouteLocale`.
* [x] **QA-05** Переписать или удалить `tests/e2e/frontend.e2e.spec.ts`. *[S]* — переписан (PR #39):
  boilerplate-ассерты заменены контрактными тестами брендинга/роутинга; в CI по-прежнему не
  выполняется (см. QA-06).
  — **аудит 2026-07-26:** хвост формулировки «в CI по-прежнему не выполняется (см. QA-06)» УСТАРЕЛ: job
  `e2e` в `test.yml` гоняет весь каталог `tests/e2e` с QA-09.
* [x] **QA-06** Подключить хотя бы `test:int` в CI (PR-гейт). *[S]* — сделано в **QA-08**
  (`.github/workflows/test.yml`, 2026-07-02)
* [~] **QA-07** Расширить + подключить в CI `tests/e2e/game-leaderboard-scroll.e2e.spec.ts`. *[S]* —
  в CI снова гоняется с **QA-09** (2026-07-02, job `e2e`); расширение обвязки — **QA-32**.

---

## Принцип исполнения

Не запускать 6 агентов сразу. Сначала один конвейер:
`crawler → находки → dashboard → approve → publish → translate` (M2-T06…T13).
Игры добавлять кусочками по мере готовности bento-хаба, не блокируя запуск сайта.
Compliance-задачи (EU-07…EU-11) дешевле сделать в M0, чем переделывать после запуска.
